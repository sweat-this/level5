# Architecture Audit

Last updated: 2026-08-02

This document is the running audit register for Level5 architecture and gameplay systems. It starts with the main problems identified during the player interaction and systems audit, then gives us a durable place to track severity, impact, recommendations, and remediation status.

Use this document for problems and decisions. Use [Systems and Architecture Baseline](systems-architecture-baseline.md) for the current system map.

AUD-022 through AUD-033 came out of the 2026-08-06 deep audit and are tracked, with their fixes, in
[Deep Audit 2026-08-06](deep-audit-2026-08-06.md) rather than duplicated into the table below. All
twelve (AUD-022 to AUD-033) are fixed in code and pending Unity compile/playtest verification. A
second pass added AUD-034 to AUD-037 and a third added AUD-038 to AUD-039, also fixed and awaiting
Unity compile/playtest verification. A fourth pass added AUD-040 to AUD-043 (stats-screen paging) and a fifth added AUD-044 to AUD-045
(combat ownership and account identity), also fixed. All twenty-four await Unity compile/playtest
verification.

AUD-057 through AUD-063 came out of the 2026-08-09 deep audit and are tracked, with their fixes, in
[Deep Audit 2026-08-09](deep-audit-2026-08-09.md). All seven are fixed; 293 edit-mode and 4 play-mode
tests pass. Two carry follow-ups: AUD-057 needs token TTL/revocation confirmed against
`Level5Backend`, and AUD-059's structural half - the `Assets/Scripts` assembly split, which is the
still-open part of AUD-012 - remains outstanding. What AUD-059 did fix is the consequence that was
blocking everything else: runtime gameplay code can now be covered by play-mode tests, and now is.

AUD-065 through AUD-073 came out of one or more 2026-08-14 fresh sweeps targeting areas the prior
passes had covered lightly (the racing minigame, camera, menu/dialogue flow, audio, touch-input
controllers, and any remaining unguarded `GameObject.Find`); all are fixed and in the table below
rather than a separate deep-audit file, since these were narrower, targeted passes rather than a full
re-audit. AUD-013's remaining controller-duplicate-field half was also closed the same day (see its
row). All fixes verified via full compile + 401/401 edit-mode + 4/4 play-mode +
`scripts/validate-repository.ps1`, run after each round (five rounds total this day). AUD-065
(basketball streak-bonus), AUD-070 (goal-camera desync) and AUD-068 (tip-dialogue reset) each still
need a manual Play Mode check before shipping - see their rows/backlog entry for why. AUD-068 and
AUD-071 were initially left open (AUD-068 pending a product-intent call this repo can't answer alone,
AUD-071 because a partial fix would have been worse than none) and were fixed in a follow-up pass the
same day once the user explicitly asked for every finding to be fixed regardless. AUD-072/AUD-073 came
from a systematic look at every remaining unguarded `GameObject.Find(...).member` chain in
`Assets/Scripts`; most of the ~130 call sites were left alone because they resolve objects (like
`messageDisplay`, `shot_marker_stats`, `range_slider_*`) used so pervasively on every shot/every frame
that a genuine gap would already be crashing the shipped game constantly - AUD-072/073 were the ones
that stood out as narrow, infrequently-exercised paths (a rarely-visited stats screen; touch-input-only
controllers with no automated coverage) where a real gap plausibly could hide unnoticed.

AUD-074 came out of a 2026-08-15 sweep of coroutine lifecycle and unguarded-singleton patterns not
covered by the prior passes (event-subscription pairing, input-map ref-counting, and the URP migration's
C# surface were also checked this session and found clean - no new findings there). A background
research pass proposed several candidates; each was re-verified against the actual call sites before
anything was touched, and most (retry/double-apply paths, other singleton access, other coroutines) were
confirmed already well-guarded by prior passes. Verified via full compile + edit-mode + play-mode +
`scripts/validate-repository.ps1`.

AUD-075 and AUD-076, same 2026-08-15 session, came from a wider directory sweep afterward
(`fans/`, `SFX manager/`, `Models/`, `versus/`, `database/`, `analytics/`, `character_specific/`,
`vehicle/`) that turned up two unguarded-lookup/missing-lock gaps in code confirmed, by GUID/caller
grep, to have no live reachable path today - initially recorded as housekeeping rather than fixed
(consistent with how the `RacingVehicleCollisions.cs` finding was handled), then fixed anyway on
explicit instruction to fix all findings regardless of current reachability.

AUD-084 through AUD-087 came from a dedicated 2026-08-17 audit of `Assets/Scripts/database/`
(`DBHelper.cs`, `DBConnector.cs`, `PendingMatchPersistenceStore.cs`) requested as a refactor/
optimization pass rather than a bug hunt, after reading `docs/persistence-boundaries.md` per
AGENTS.md. `PendingMatchPersistenceStore.cs` had no findings. The other two files did: duplicated
and drifted `CREATE TABLE` definitions, two entirely dead table-creation methods, per-call SQLite
connections opened and disposed roughly 40 times across both files with no concurrency to justify
it (every caller already serializes through `databaseLocked`), and an inconsistent lock-release
style that already caused two real bugs earlier this session (AUD-076). Fixed in full, including
the higher-risk connection-sharing change, per explicit instruction to fix all findings - this
touches every local-persistence read/write path in the game (accounts, scores, progression,
cheerleader/character unlocks) with no automated test coverage of actual SQLite behavior, so it
was done as a careful, individually-verified migration (never a blind file-wide rewrite) with a
full compile + EditMode + PlayMode + `validate-repository.ps1` pass after each stage. **Still
needs a manual playtest** covering account creation/login, score submission, all-time stats,
character/cheerleader unlocks, and progression saves before shipping - none of that is exercised
by the automated suite.

AUD-088 through AUD-112 came out of a dedicated 2026-08-17 audit of the UI menu system and the ten
menu scenes in build settings, requested as a review-twice pass and tracked, with its two review
passes and its evidence, in [UI Menu System Audit 2026-08-17](ui-menu-audit-2026-08-17.md) rather
than duplicated into the table below. **All twenty-five are open; nothing has been changed in code.**
The headline is that six of the ten menu screens are authored in binary-serialized prefabs despite
the project being on Force Text, so no change to them is reviewable (AUD-088) - which is why the
phased plan in [UI and Menu Architecture](ui-input-architecture.md) starts with reserialization
before anything behavioural. The rest cluster into one input route (the UI module runs Unity's
default actions, not `PlayerControls.UINavigation`), one behaviour route (polled directional input
racing the module; pause and progression controls a mouse cannot operate), one wiring route (three
competing mechanisms, five copies of the footer), one scaling contract (three canvases at three
reference resolutions in a single screen), and the stats table's shared-prefab mutation and
undefined row order - the latter being the same defect class as the closed AUD-020. The
implementation prompt is [here](ui-menu-overhaul-implementation-prompt.md). The second review pass
withdrew three Pass 1 candidates (a start-menu double-invoke that turned out to be frame-guarded, a
`StandaloneInputModule` crash that `activeInputHandler: 2` prevents, and active git corruption of
the binary prefabs that `git ls-files --eol` disproves); those are recorded in the audit so they are
not re-raised.

## Audit Status Legend

- Open: Known issue with no durable fix yet.
- In Progress: Fix has started but follow-up work remains.
- Mitigated: Immediate risk has been reduced, but the architecture should still improve.
- Closed: Durable fix is complete and verified.

## Main Problems

| ID | Area | Severity | Status | Problem | Impact | Recommended Solution |
| --- | --- | --- | --- | --- | --- | --- |
| AUD-001 | Combat state ownership | High | Mitigated | Combat state is split across player controller, collision handlers, health scripts, animation events, UI, and the attack queue. | Confirmed concretely: `EnemyCollisions.enemyIsDead` and `EnemyController.enemyIsDead` were two independent copies of the enemy-death side effects that had **already diverged** - the same kill healed the player 5/2 (boss/minion) through the collision path but 7/3 through the controller path. | Fixed: `EnemyController.HandleDeath(attacker, creditToPlayer)` is the single owner of enemy death - heal, kill credit, critical flourish, and the death coroutine. Both call sites delegate. Heal amounts unified on 5/2 (the melee path, which is what the game has always felt like) and named as constants. `creditToPlayer` is explicit rather than inferred from a null attacker, because the two originals disagreed on whether friendly fire rewards the player - it does not. `ActorHealth` (AUD-004) already owns the death latch, and the health bars already subscribe to `OnHealthChanged`. **Still open**: `PlayerCollisions` retains damage + controller state + UI + audio in one place, and attack definitions are still per-actor-type `PlayerAttackBox`/`EnemyAttackBox` components. **New finding 2026-08-13, resolved by product decision.** `PlayerCollisions` (human, tag `playerHitbox`) and `AutoPlayerCollisions` (CPU, tag `autoPlayerHitbox`) are a human/CPU duplicate pair like the ones already fixed elsewhere this pass (AUD-002, AUD-017), and had diverged further than those did: the CPU path was missing disintegrate-attack handling, dunk-sequence triggering (its block was commented out entirely), and the sniper-mode/instant-kill-projectile attack triggers the human path has. Confirmed with the user rather than guessed at: **CPU players are not meant to dunk** (left as-is, dunk block still commented out in `AutoPlayerCollisions`) **but can be disintegrated** (fixed). `AutoPlayerController` gained the `Disintegrated` property, the matching `Update()` poll, and a `PlayerDisintegrated()` coroutine in `AutoPlayerDamageReactions.cs`, mirroring the human path exactly - `AutoPlayerController`'s movement gating already excluded `disintegratedState`, so the animator/movement side was half-built already. `AutoPlayerCollisions` gained `isDisintegrate` detection and a `playerDisintegrated()` helper using its **existing** trigger condition - the sniper-mode/instant-kill-projectile trigger expansion was intentionally *not* added, since that wasn't part of the confirmed decision and is a separate open question if disintegrate attacks need to reach CPU through those specific sources too. `PlayerCollisions.GetPlayerObjects()` still branches on `playerIdentifier.isCpu` to resolve an `autoPlayerController` field that `OnTriggerEnter` never reads (gated out by the `playerHitbox` tag check first) - vestigial, not dangerous, left alone rather than guessed at without a scene/prefab check confirming no path ever attaches `PlayerCollisions` to a CPU hitbox object. **2026-08-16: that scene/prefab check was finally done, and the guess was wrong - see AUD-083.** All 22 `cpu_players/*.prefab` files did exactly that: `PlayerCollisions` (not `AutoPlayerCollisions`) sat on their hitbox object, tagged `autoPlayerHitbox`, so the `playerHitbox` tag check silently gated out the entire component for every CPU opponent in the game - not vestigial, a live and long-standing combat-integrity gap. Fixed. |
| AUD-002 | Player controller scope | High | In Progress | `PlayerController` owns too many responsibilities, including movement, input handling, basketball actions, combat decisions, animation coordination, and state changes. | The player system becomes hard to test, risky to change, and difficult to reuse across modes. | Split player behavior into focused components: movement motor, combat driver, basketball driver, input adapter, animation bridge, and state facade. **2026-08-13:** input reading already routed through `PlayerInputReader` before this pass. Added the first genuine seam: damage/knockdown/lightning/shrink reactions extracted to `PlayerDamageReactions` (human) and `AutoPlayerDamageReactions` (CPU, matching subset only), both controllers kept as facades with unchanged public signatures. Motor, combat-driver, and basketball-driver extraction remain undone - this is a start, not the split. No test coverage exists for either extracted area and neither has been manually playtested. |
| AUD-003 | Global/static dependencies | High | Open | Broad use of global managers and static option/state holders creates implicit dependencies. | Systems depend on scene load order and hidden global state, making tests, prefab reuse, and mode-specific behavior more fragile. | Introduce an explicit scene-owned runtime context for gameplay services. Keep globals temporarily as compatibility bridges while new systems migrate to explicit references. |
| AUD-004 | Duplicated actor health | High | Mitigated | Player, enemy, and bodyguard health/death behavior is implemented separately. | Damage, invulnerability, UI updates, death cleanup, and scoring reactions can diverge between actor types - and had: `BodyGuardHealth`'s max-health property was still literally named `MaxEnemyHealth`, and the two spelled the hardcore bonus differently. | Fixed: `Assets/Scripts/combat/ActorHealth.cs` owns the clamping `Health` setter, the `MaxHealth` setter, the death latch, `ApplyDamage`/`Heal`/`TakeDamage`, and both events. `EnemyHealth` and `BodyGuardHealth` now derive from it and implement only how max health is chosen (spawn-role scaling vs. a flat default). `PlayerHealth` deliberately stays separate - it carries block/special/respawn state no AI actor has - but already implemented `IDamageable` with the same `Health`/`MaxHealth`/`IsDead` vocabulary, so all three now read alike. `[FormerlySerializedAs]` preserves prefab values across the field rename. Covered by 10 new edit-mode tests. |
| AUD-005 | Attack queue coupling | Medium | Mitigated | The attack queue now handles reservations more safely, but it still knows about concrete enemy/bodyguard behavior patterns. | Adding new melee actor types required editing the queue in three places. The two detection components held the same three fields but named the sighting flag differently (`PlayerSighted` vs `EnemySighted`), so the queue carried four near-identical blocks to set them. | Fixed: new `ICombatDetection` (`Attacking`, `AttackPositionId`, `TargetSighted`) is implemented by `EnemyDetection` and `BodyGuardDetection`, which keep their existing names as the backing properties. `PlayerAttackQueue` now sets reservation state through one `SetAttackerDetection` helper and asks `CanReserve` through the interface; it no longer names either concrete type. Reservation/release itself was already `ICombatAgent`-based. The pass-two note that `RefreshBodyGuards` only ran in `Start` is resolved separately - `BodyGuardController` self-registers on enable and unregisters on disable/death. **Still open**: the class is still named `PlayerAttackQueue` and still lives on the player, rather than being a scene-level `CombatSlotReservationSystem`. |
| AUD-006 | Animation event authority | Medium | Mitigated | Animation events still participate in important gameplay timing and cleanup. | Renamed, missing, or mistimed animation events can break attacks, projectile firing, recovery, or state reset. | Treat animation events as presentation/timing hints. Gameplay code should validate state, provide fallbacks, and own cleanup on disable/death/interrupt. |
| AUD-007 | AI state complexity | Medium | Mitigated | Enemy, bodyguard, CPU player, and special NPC behavior rely on local booleans and bespoke checks. | Behavior changes become fragile because state transitions are implicit and scattered. | **2026-08-13 update:** substantially more progress exists than this row previously showed - a prior pass (commit `0e001a60`, in-code `STEP N/17` markers across `EnemyController.cs`/`BodyGuardController.cs`/`CombatTargetSelector.cs`/`EnemyDetection.cs`/`BodyGuardDetection.cs`/`EnemySpawner.cs`) gave both actor types explicit, lazily-resolved target references (replacing raw queue order and `GameLevelManager.instance` reaches), a shared `CombatTargetSelector` scoring policy (nearest-valid for enemies, tiered threat score for bodyguards), and a `CombatTacticalState` enum with transition bookkeeping (`CombatTacticalStateTransitions.TryCommit`) that both controllers already project their boolean flags into every frame via `UpdateAiStateDiagnostics()`. That projection is **deliberately read-only** - the commit message is explicit that it is "diagnostics (read-only, no per-frame logging)", not step one of a takeover - so `stateWalk`/`stateAttack`/`statePatrol`/`stateKnockDown` remain the actual behavior owners on purpose, not by omission. Making the enum authoritative was never approved scope and was not attempted. What was found and fixed this pass: `Level5CombatTargetSelectorTests.DestroyedCandidateIsRejectedNotThrown` was failing - not a production bug, the test itself called `.gameObject` on a `Component` after destroying it. `CombatTargetSelector.IsValidCandidate`'s actual destroyed-object guard was already correct. Recommended next move, if this is picked up again: decide *with the user* whether `CombatTacticalState` should ever become authoritative before touching the boolean-driven behavior - that is a product/feel decision, not a refactor, given zero existing test coverage of enemy/bodyguard runtime behavior and no ability to manually playtest it here. |
| AUD-008 | UI/gameplay coupling | Medium | In Progress | Several UI flows share ownership of gameplay or progression state instead of only rendering it. | UI changes can accidentally change gameplay behavior, and gameplay changes can break menu/result presentation. | **2026-08-13 update:** more done than this row showed. A prior pass (commit `3dc69b76`) extracted `MatchHudPresenter` from `GameRules` (1407 -> 743 lines) behind an `IsComplete` boundary nothing outside presentation can read, fixing a real bug where a renamed HUD object silently skipped saving match results. That extraction is a straight relocation (formatting unchanged), not event-driven - `GameRules` still calls into it directly. This pass added the first real subscriber to `BasketBallShotMade.ShotResolved` (the AUD-010 event, previously published with zero listeners): the made-shot swish sound and rim animation now fire from `PlayMadeShotPresentation`, subscribed in `Start()`, instead of running inline inside the scoring method - same object, same frame, so behavior is unchanged, but the event now demonstrably works end to end. `GameStats` and the shot-marker `ShotMade` counter remain direct-call, correctly so - both feed win-condition/scoring state (`GameRules.IsGameOver()` reads the marker counter directly) computed as part of resolving the shot, not a presentation reaction to it, so neither is actually an AUD-008 case on closer inspection. `MatchHudPresenter`'s scoreboard is not called from the shot-made path at all - `GameRules.Update()` polls it every frame independent of shots, reading live `GameStats`/rules values; that is a polling-vs-push question, not "UI reaches into gameplay," and moving it to a `ShotResolved` subscriber is a design decision (other HUD-visible state like timers changes without a made shot) rather than a mechanical extraction. No further zero-risk slice is available in this exact area without that decision. |
| AUD-009 | Spawn/instantiate lifecycle | Medium | Mitigated | Projectiles have pooling, but enemies, vehicles, pickups, effects, and other high-churn objects do not appear to share a standard lifecycle. | The premise is now out of date - enemies (`EnemySpawner`) and vehicles (`TrafficManager`) both pool through `RuntimeObjectPool`. The live gap was that reset happened purely by `OnEnable` convention, which nothing declared or enforced, and `RuntimeObjectPool.Spawn` ran the caller's `configure` callback *before* activation, so any type that reset a field its caller configures would silently discard it. | Fixed: new `IPooledSpawnReset` makes the contract explicit, and `Spawn` invokes it on the inactive instance **before** `configure`, so reset clears the previous life, configure applies this one, and activation lets `OnEnable` see both. `EnemyController` implements it (and cascades to `EnemyHealth`); the interface documents that only the prefab's root coordinator should, since implementing it on both would reset health three times per spawn. `Spawn` also gained the null guards `Release` already had. The ordering bug was latent, not live - `VehicleController.ConfigureRoute` does not touch the configured `Direction`/`CurrentTarget`. **Investigated further 2026-08-13.** `ProjectilePool` remains a second, independent implementation - a forced merge was rejected: projectiles need a post-activation `Launch()` step (arming/starting travel) that `RuntimeObjectPool` has no equivalent for, and the two prefab-keyed dictionaries are never confused in practice because nothing spawns the same prefab through both. What full investigation found instead was a real, separate bug: unlike `RuntimeObjectPool`, `ProjectilePool`'s static dictionary had no scene-unload/domain-reload cleanup, so it silently accumulated pools referencing instances destroyed with their scene on every load. Fixed by mirroring `RuntimeObjectPool`'s existing `[RuntimeInitializeOnLoadMethod]` + `SceneManager.sceneUnloaded` pattern. |
| AUD-010 | Basketball flow ownership | Medium | In Progress | Basketball shot state, scoring, range/shot meters, stats, game rules, and player actions are spread across multiple systems. | Makes/misses, score updates, UI feedback, and progression stats can drift if the flow changes. | Shot lifecycle is documented, scoring/modifier arithmetic is extracted, and `BasketBallShotMade` now publishes `MadeShotResult`. Still open: move `GameStats`, HUD, audio, marker updates, progression and game rules behind subscribers. **2026-09-01, Phase 1c**: the `GameStats -> BasketBallState` edge is cut - `GameStats` is now a pure compatibility facade over `MatchStats` with no scene-type dependency, guarded by `Level5GameStatsDependencyGuardTests`. **2026-09-01, Phase 1c addendum**: `RangeMeter` is now actor-owned basketball presentation, explicitly bound by `SpawnCoordinator.BindRangeMeters` during participant composition instead of reading `GameLevelManager.instance.players[0]`/`PlayerIdentifier` itself; it consumes `IShooterActor` only, guarded by `Level5RangeMeterArchitectureGuardTests`. The same pass also fixed a pre-existing bug where `Start()` scheduled its `InvokeRepeating("setSliderValue", ...)` presentation refresh twice (once unconditionally, once again if still active after visibility gating) - it now schedules once. **2026-09-01, Phase 1c ShotMeter slice**: `ShotMeter` is now actor-owned basketball presentation too, explicitly bound by `SpawnCoordinator.BindShotMeters` during participant composition instead of reading a parent `PlayerIdentifier`; it consumes `IShooterActor` only, guarded by `Level5ShotMeterArchitectureGuardTests`. A CPU's automatic meter resolution needs its own basketball, which is bound separately and optionally through a new `ShotMeter.BindBasketballRuntime(IBasketballRuntime)`, called by `SpawnCoordinator.GiveBall` once that participant's ball exists - a defensive/no-ball CPU never receives one and stays a valid, actor-bound `ShotMeter`. The CPU roll itself (`BasketBallAuto.rollForAutoPlayerSliderValue`, including its exact random-draw order and formulas) is unchanged; `ShotMeter` now reaches it through the bound runtime, cast to `BasketBallAuto` (the same-domain concrete type, not a new abstraction) rather than through `playerIdentifier.basketBallAutoController`. **2026-09-02, Phase 1c ground-height slice**: `BasketBall`'s no-active-Terrain drop-shadow fallback no longer reads `GameLevelManager.instance.TerrainHeight` directly - it receives a `Level5.Core.IGroundHeightProvider` through explicit composition (`SpawnCoordinator.GiveBall` -> `BasketBall.BindGroundHeightProvider`, called once, before `Start()`), which `GameLevelManager` implements over its existing `TerrainHeight` value (`float IGroundHeightProvider.GroundHeight => terrainHeight;`). The value is read live at the point of use (`BasketBall.ResolveDropShadowHeight`), never cached, since `GameLevelManager.Start()` updates `terrainHeight` from the primary participant's actual Y after spawning - a bound reference has to observe that later value. `SpawnCoordinator`'s constructor takes the provider as an optional parameter so every existing direct-construction call site (tests exercising `RegisterHuman`/`RegisterCpu`/`GiveBall` without a running `GameLevelManager`) keeps compiling unchanged; a human ball spawned through a provider-less coordinator fails closed in its own `Start()` (same shape as a missing owner) rather than inventing a fallback. CPU composition (`BasketBallAuto`) is unaffected - only the human ball consumes this dependency. `Assets/Scripts/basketball` now has zero live `GameLevelManager` references, guarded by `Level5BasketballGameManagerEdgeTests` (scoped to `GameLevelManager` only - the separate `basketball -> GameRules` dependency remains unresolved). **2026-09-03, Phase 2b0 RangeMeter slice**: `RangeMeter` no longer reads `MatchRuntime.Rules`/`MatchRuntime.HasConfiguration` itself - it receives a `Level5.Core.Match.ResolvedMatchRules` plus a configuration-presence flag through a new `RangeMeter.BindMatchContext(ResolvedMatchRules, bool)`, bound by `SpawnCoordinator.BindRangeMeters` alongside actor ownership (mirroring `ShotMeter.BindMatchRules` from the preceding Phase 2b0 slice, PR #88). Configuration presence is captured once, in `SpawnCoordinator`'s own constructor - mirroring how it already owns `rules` - rather than re-read from `MatchRuntime.HasConfiguration` on every `BindRangeMeters` call; `GameLevelManager.Awake` always constructs the coordinator after `ActiveMatch` is already established for the scene load and before any participant is spawned, so this is a structural guarantee rather than one resting on repeated reads happening to agree. The exact five-term human visibility predicate (`Hardcore`/`EnemiesOnly`/`IsBattleRoyal`/no active configuration/`AllowsCpuShooters`) and the CPU-exemption gate are both preserved unchanged. `Assets/Scripts/basketball/RangeMeter.cs` now has zero live `MatchRuntime` references, guarded by an extended `Level5RangeMeterArchitectureGuardTests`. `Level5.Basketball` remains unasmdef'd - `BasketBall`, `BasketBallAuto`, `BasketBallShotMade`, `BasketBallShotMarker`, `BasketballShotPipeline` and `BasketballState` still carry live `MatchRuntime`/`GameRules`/concrete-player-type references, which this slice does not touch. **2026-09-03, Phase 2b0 BasketBallState slice**: `BasketBallState` no longer reads `MatchRuntime.Rules.RequiresBasketball` in `Update()` - it receives the match's already-resolved `ResolvedMatchRules` through a new `BasketBallState.BindMatchRules(ResolvedMatchRules)`, bound by `SpawnCoordinator.GiveBall` immediately after `IBasketballRuntime.BindOwner`, mirroring the existing `GameStats.BindMatchRules` call on the same method. `BindOwner`'s existing owner/role semantics and `Bound`'s meaning are unchanged; match-rules binding is a separate, independently-guarded composition step (null and second-bind attempts are rejected with an actionable error, matching `BindOwner`'s own rebind guard). `Start()` now fails closed - deactivating the whole basketball `GameObject`, not just this component, since `BasketBall`/`BasketBallAuto` share it and depend on this component's range/shot state - when match rules were never bound, the same shape it already used for a missing owner. The exact `RequiresBasketball=false` no-op (no state is cleared) and the exact point/range distance thresholds and operators (including the pre-existing gap at exactly `DISTANCE_7point`, where neither `FourPoints` nor `SevenPoints` is set) are preserved unchanged and pinned by a new `Level5BasketBallStateUpdateTests`. `Assets/Scripts/basketball/BasketballState.cs` now has zero live `MatchRuntime` references, guarded by a new `Level5BasketBallStateDependencyGuardTests`. **2026-09-03, Phase 2b0 BasketBallAuto slice**: `BasketBallAuto`'s last two live `MatchRuntime` reads (`Start()`/`Update()`'s `MatchRuntime.Rules.EnemiesOnly`) are replaced the same way - a new `BasketBallAuto.BindMatchRules(ResolvedMatchRules)`, bound by `SpawnCoordinator.GiveBall` immediately after `IBasketballRuntime.BindOwner`, mirroring `BasketBallState.BindMatchRules`'s bind-once/null-guard/no-rebind shape exactly (checked-before-null-argument ordering included, so a null second bind after a real one still reports "already bound"). `Start()` now validates the bound rules between the existing owner check and `instance = this`, failing closed (deactivating the whole `GameObject`, same shape as the missing-owner branch) rather than letting an invalid ball claim the static. The exact `EnemiesOnly=true` startup effects (`Y += 20`, `Rigidbody.constraints = FreezeAll`) and `Update()`'s ordering (`CheckIsBallFacingGoalAuto()` unconditional, before the gate - never moved into the normal-ball branch) are preserved unchanged; human `BasketBall`'s extra `EnemiesOnly`-gated behavior (`IsBattleRoyal` check, drop-shadow deactivation) was deliberately not copied over, since `BasketBallAuto` never had it. `GameOptions.EnemiesOnlyEnabled` has no production writer during an active match (only `LegacyGameOptionsBridge`, pre-scene-load, and a test snapshot helper), so a fixed bind-once rules reference is behavior-preserving for both a configured launch and a directly-entered scene. `Assets/Scripts/basketball/BasketBallAuto.cs` now has zero live `MatchRuntime` references, guarded by a new `Level5BasketBallAutoDependencyGuardTests`. `BasketBall`, `BasketBallShotMade`, `BasketBallShotMarker` and `BasketballShotPipeline` still carry live `MatchRuntime`/`GameRules`/concrete-player-type references, which this slice does not touch; `BasketBallAuto` also still reaches two Assembly-CSharp types outside `Assets/Scripts/basketball` (`SFXBB`, `character_specific/BehaviorNpcCritical`), the next smallest blockers toward an eventual `Level5.Basketball.asmdef`. **2026-09-04, Phase 2b0 BasketballShotPipeline slice**: `BasketballShotPipeline.ApplyMarkerAndMoneyBallOnShoot`'s last `MatchRuntime.Rules` read is replaced the same way - it now takes the match's already-resolved `ResolvedMatchRules` as an explicit third parameter, supplied by each caller's own bound `matchRules` field (`BasketBall`/`BasketBallAuto`, each already bound once by `SpawnCoordinator.GiveBall` - see the `BasketBallAuto` slice above; `BasketBall`'s equivalent bind landed in PR #92 without its own row addendum) rather than re-resolving an equivalent value from `MatchRuntime` a second time. A null argument is treated as a composition bug and fails closed - logging an actionable error and mutating no marker or money-ball state - before the existing no-`CurrentShotMarker`/no-`IMoneyBallState` guards run. The marker-required gate, contest fifth-attempt accounting, and live `IMoneyBallState` read are all preserved unchanged. `Assets/Scripts/basketball/BasketballShotPipeline.cs` now has zero live `MatchRuntime` references, guarded by a new `Level5BasketballShotPipelineDependencyGuardTests`. `BasketBallShotMade` and `BasketBallShotMarker` remain the two production basketball files left with live `MatchRuntime`/`GameRules`/concrete-player-type references, which this slice does not touch - `BasketBallShotMade` also depends on mode identity (`MatchRuntime.RawModeId`) and needs its own separate audit rather than the simple rule-parameter handoff used here. |
| AUD-011 | Persistence boundaries | Medium | Mitigated | Account, local save, server messages, database/API calls, progression, and migration systems need clearer source-of-truth documentation. | Writing it down surfaced a concrete ambiguity: there are **two parallel progression stores**. SQLite is authoritative and live; the per-account JSON store is read but never seeded, because `CharacterProgressStore.Load` has no callers and `CharacterProgressMigration.FromLegacyRecords` is entirely orphaned. Not a live bug - both readers check SQLite-backed data first and only fall through to JSON - but it looks like a source of truth, and reordering those checks would return empty progress for every existing player with no error. | Documented in [Persistence and Account Identity](persistence-boundaries.md): the three stores and which is authoritative, the difference between `GameOptions.userid` (a local selection), `APIHelper.HasSession` (a real session), and `CharacterProgressAccountId` (a filesystem scope), the queue/retry path for every write, `AtomicFile` and DB-locking behaviour, and the two client/server trust assumptions to confirm against `Level5Backend`. **2026-08-14: closed.** `CharacterProgressMigration` (the written-but-never-called legacy-record migration) was deleted rather than wired - it had no callers, no test coverage, and no confirmed legacy-record source to migrate from. |
| AUD-012 | Legacy/dev/test separation | Low | Mitigated | Production scripts, test scripts, diagnostics, original managers, and utility helpers are mixed in the runtime tree. | Old or diagnostic code may be accidentally referenced by production systems - confirmed, not theoretical: three production files referenced dev code, and two more dead files (1683 + 311 lines) sat in the runtime tree. | Fixed: `Assets/Scripts/Dev/` now holds `DevFunctions`, `CharacterProgressParityLogger`, `AutoPlayerControllerTest`, `TestText`, `BasketballTestStats`, `BasketBallTestStatsConclusions`; the two live production call sites are wrapped in `#if UNITY_EDITOR \|\| DEVELOPMENT_BUILD`; the fully-commented `BasketBallShotMarkerAuto.cs` joined `StartManager_original.cs` in a `Legacy~` folder. `Level5ProjectValidator.CollectDevIsolationErrors()` + a new edit-mode test fail the build on any unguarded production→Dev reference. **Still open**: the `.asmdef` split of `Assets/Scripts` - see the note below the table. |
| AUD-013 | Player identity/role ownership | Medium | Closed | `PlayerIdentifier.isCpu`/`isDefensivePlayer` are duplicated independently on `PlayerController`/`AutoPlayerController`, and each player slot has two hand-synced `PlayerIdentifier` instances (actor + basketball object). | A new spawn/mode path that sets one flag copy and forgets the other silently splits CPU-detection or defensive behavior between subsystems, with no validation. | Fixed: the known runtime reads in `BasketBall.cs`, `groundcheck.cs`, and `AutoPlayerController.cs` now go through `PlayerIdentifier`. **2026-08-14:** grepped every remaining reader/writer of the controller-owned duplicates and confirmed all of them were dead - `PlayerController.isCPU`/`.pid`/`.bid` (and its `Pid`/`Bid` properties) and `AutoPlayerController.isCPU`/`.isDefensivePlayer` had no readers anywhere in the tree, only a `GameLevelManager` writer for the two `isCPU` copies that nothing ever consumed. Deleted all of them rather than leaving them "kept for serialization safety" - Unity drops now-unknown serialized values silently, no prefab/scene edits required. Also deleted `PlayerIdentifier.bid`/`bsid`, which were always set equal to `pid` at every `setIds` call site and never read; `setIds` is now `(int pid, bool isCpu)`. **Confirmed independently**: `dev` PR #50 ("Fix autonomous actor defects across CPU, enemy, NPC and cheerleader systems", commit `b762e5d4`) reached the identical conclusion about `AutoPlayerController.isCPU`/`isDefensivePlayer` on its own branch, citing this same AUD-013 row, and removed them with its own "CPU-6" note - landed on `dev` while this row's fix was still local and uncommitted. Reconciled via `git stash` / fast-forward / `git stash pop` rather than picking one side: the two field-removal diffs were identical in substance (kept PR #50's more detailed comment over this session's own), and every other file both touched (`GameLevelManager.cs`, `SpawnCoordinator.cs`, `PlayerIdentifier.cs`) merged cleanly since the edits landed on non-overlapping lines. Re-verified after merging: 408/408 EditMode (404 this session's + 4 from PR #50's new `Level5AutonomousActorTests.cs`), 4/4 PlayMode, `validate-repository.ps1` clean. **2026-08-31: the remaining structural half is closed.** A new basketball-domain contract, `IBasketballRuntime`, is implemented directly on `BasketBall`/`BasketBallAuto` and bound explicitly by `SpawnCoordinator.GiveBall` immediately after `Instantiate` (participant id, CPU role, primary status, owner actor, `IShooterActor`), before Unity calls any of the ball's components' `Start()`. `BasketBall`, `BasketBallAuto`, `BasketballState`, `BasketBallShotMade` (now `shotMade(IBasketballRuntime)`), and `BasketBallShotMadeCollision` no longer reference `PlayerIdentifier` at all. `PlayerIdentifier` was then removed from both `basketball.prefab` and `basketballAuto.prefab` via `PrefabUtility` (not hand-edited YAML) - each was a clean, single-component removal with no other prefab drift. The actor-side `PlayerIdentifier` (reached through `PlayerRegistry`) remains the single authoritative participant identity; nothing on the basketball object duplicates it anymore. Two new architecture-guard tests pin this down permanently (`Level5BasketballRuntimeIdentityGuardTests`: zero `PlayerIdentifier` references in the five migrated files, zero `PlayerIdentifier` components in either prefab) alongside 8 new behavioral tests (`Level5BasketballOwnershipBindingTests`) driving the real `SpawnCoordinator.GiveBall` and production prefabs. Verified: full compile + 714/714 EditMode + full PlayMode, all via Unity 6000.5.7f1 batch mode, plus `validate-repository.ps1` clean. |
| AUD-014 | Player slot index used as save-data key | High | Mitigated | `HighScoreModel` derives which player's stats to save by looping over `PlayerIdentifier` entries and overwriting an index variable for every non-CPU entry, then reads back by that index; other code assumes `pid` always equals list position. | With more than one human player, or if `pid`/list-position ever diverge, this silently saves/highscores the wrong player's stats - a save-data correctness bug, not just a smell. | Code fix in place: `convertBasketBallStatsToModel` now uses `GetPrimaryPlayer` and `TryGetPlayer`. Pending: Unity compile/playtest verification. |
| AUD-015 | Basketball shot-attempt flags never reset on miss | High | Mitigated | `TwoAttempt`/`ThreeAttempt`/`FourAttempt`/`SevenAttempt` flags are set true when a shot is launched but only ever cleared inside the make path - nothing resets them on a miss. | A player who misses a 2pt attempt then makes a 3pt attempt gets scored for both categories at once - a reachable scoring-integrity bug, not theoretical. | Code fix in place: `BasketballState.ResetShotAttemptSnapshot()` clears attempt, marker, and moneyball snapshot state and is called at the start of each human/CPU attempt. Pending: Unity compile/playtest verification. |
| AUD-016 | Basketball percentage stats can read the wrong player | Medium | Mitigated | `GameRules.GetStatsTotals` mixed one player's make/attempt counts with `BasketBall.instance`'s accuracy getters, but `BasketBall.instance.gameStats` is reassigned inside `updateScoreText` to whichever player last triggered a score update. | In local-multiplayer modes, a displayed shooting percentage could silently come from a different player's accuracy state than the made/attempt counts sitting next to it. | Code fix in place: percentages now compute from `gameStats1` counts via `UtilityFunctions.getPercentageFloat`, with no `BasketBall.instance` dependency. Pending: Unity compile/playtest verification. |
| AUD-017 | `BasketBall`/`BasketBallAuto` duplicate the shot pipeline | Medium | Mitigated | `shootBasketBall`, `Launch`, modifier math, and score-text formatting are copy-pasted between the human and CPU basketball scripts, and have already diverged (different meter-wait condition, missing money-ball block in the auto path). | Every shot-logic fix, including AUD-015/AUD-016, must be applied twice or the human/CPU paths silently diverge further. | First slice landed 2026-08-07, tests-first (modifier math into `Level5.Core.ShotModifiers`, 13 characterization tests). **2026-08-13**: `Launch`'s full modifier computation and score/profile-text formatting unified into `BasketballShotPipeline.cs`, both callers now thin wrappers - see docs/shot-lifecycle.md. The two known divergences were resolved by explicit product decision rather than guessed at: the money-ball/marker block, previously commented out in the CPU path, is now shared via `BasketballShotPipeline.ApplyMarkerAndMoneyBallOnShoot` (built from `BasketBall`'s current live block, not `BasketBallAuto`'s stale commented copy, which had already drifted further - missing the seven-point-contest clause); the opposite `MeterEnded` wait conditions in each `LaunchBasketBall` coroutine were confirmed intentional and left untouched. **Still open**: `shootBasketBall`'s non-marker body and `Launch`'s Rigidbody/analytics/trigger-reset tail remain per-caller (correctly - human has an analytics call and an `isCpu` swish gate the CPU path doesn't, CPU has `shootTrigger`/`Locked` resets the human path doesn't). |
| AUD-018 | Game mode dispatch duplicated with inconsistent constants | Medium | Mitigated | `GameRules.SetScoreDisplayText` and `GameRules.GetDisplayText` each implement their own long if-chain over the same mode set, one using named `Modes` constants and the other raw integer literals. | Adding, renaming, or reordering a mode means editing two independently-maintained literal representations that can silently drift apart. | Fixed the inconsistency: both methods (plus `GameRules.Update()`) now use `Modes` constants only, no raw literals for named modes. Still two separate if-chains, not one lookup - full consolidation deferred as too risky without test coverage. |
| AUD-019 | Match-end side effects run on a per-frame poll | Low | Mitigated | **Corrected 2026-08-02**: this entry originally claimed `GameRules.IsGameOver()` was dead code competing with a live `Timer` path - that was wrong (see Finding Details). The real, narrower issue: end-of-match side effects ran inside `GameRules.Update()`'s per-frame poll rather than a fire-once flow. | Per-frame polling of end-of-match side effects (persistence, progression, scene load) risked re-entrancy or duplicate execution if the condition held across multiple frames. | Code fix in place: `GameRules` now routes end conditions through `RequestGameOver()`, subscribes to `PlayerHealth.OnDied`, and gates match-end side effects with `matchEndHandled`/`HandleMatchEnded()`. Pending: Unity compile/playtest verification. |
| AUD-020 | Traffic vehicles mutate shared prefab state before instantiating | Medium | Closed | `TrafficManager` set `Direction`/`FacingRight`/`CurrentTarget` directly on the shared, prefab-sourced `VehicleController` list entries before every `Instantiate()` call, in `spawnVehiclePrefabs`, `spawnCustomVehiclePrefabs`, and `spawnVehicleCoRoutine` - the exact pattern already generically flagged in AUD-009, with the original developer's own comment acknowledging it (`this is saving and changing prefabs value`). | `spawnVehicleCoRoutine` mutated the shared reference then waited (`WaitForSeconds`) before instantiating; two respawns of the same vehicle close together could race, with the second's field-writes overwriting the first's pending values on the same shared object. | Fixed: all three call sites now instantiate first and set the per-shot fields on the returned clone instead of the shared list entry. Verified `VehicleController`/`VehiclesList` have no consumers outside `vehicle/TrafficManager.cs` and `vehicle/VehicleController.cs`, so this was safe to change in isolation. |
| AUD-021 | Beat the Computahs continues never reset between campaign attempts | High | Closed | `EndRoundData.numberOfContinues` is a `static` field decremented on a loss-with-continues-remaining (`EndRoundMenuManager.cs`) and zeroed for hardcore mode (`StartManager.setGameOptions`), but nothing ever reset it back to its default when starting a fresh, non-hardcore campaign run. | A player who exhausted their continues once had zero continues on every subsequent "Beat the Computahs" attempt for the rest of the application session, with no indication why - a real, session-persistent progression bug (ties confirmed as an intentional mechanic, not a bug - not touched). | Fixed: `StartManager.setGameOptions()` now explicitly resets `EndRoundData.numberOfContinues` to `EndRoundData.DefaultContinues` for non-hardcore runs (still 0 for hardcore) on every fresh game start, instead of only ever zeroing it. `DefaultContinues` extracted as a named constant (left at 2, the pre-existing default - not a confirmed design number, so not changed) so the default lives in one place. |
| AUD-065 | "In The Pocket" streak bonus reads the pre-shot streak count | High | Closed | `BasketBallShotMadeCollision.OnTriggerEnter` called `basketBallShotMade.shotMade(...)` (which scores the shot using `gameStats.ConsecutiveShotsMade`) and only afterward called `gameStats.calculateConsecutiveShot(...)` (which updates that same field for the shot that was just made). `ShotScoringInput.ConsecutiveShotsMade`'s own doc comment says "the streak so far, including this shot" - the call order never actually included it. A second, compounding bug shared the same root cause: `calculateConsecutiveShot` reads `basketBallState.TwoAttempt` to decide whether a made two-pointer should break the streak, but by the time it ran (after `shotMade()` returned), `BasketballState.ResetShotAttemptSnapshot()` had already cleared `TwoAttempt` to `false` inside `shotMade()` - so that check could never fire. | In "In The Pocket" mode, the streak bonus activated one shot later than its configured threshold (e.g. a 3-shot threshold paid out starting on the 4th consecutive make, not the 3rd), and a made two-pointer never broke a streak the way the design comment says it should. Both bugs sit on the sole live scoring call site for every made shot, human and CPU. | Fixed: `calculateConsecutiveShot` now runs after `ShotMade` is finalized (which the streak check depends on) but before `ResetShotAttemptSnapshot()` clears `TwoAttempt` (which the streak check reads). `ShotScoring.Score` is called twice - once provisionally to learn which counter the shot moves (`CountedAs`/`MoneyBallMade` never depend on `ConsecutiveShotsMade`, confirmed by reading `ShotScoring.cs`), then again with the now-current streak value for the real `Points`. Every mode except In The Pocket is unaffected (byte-identical between the two `Score` calls) since `ConsecutiveShotsMade` only feeds `OpenPlayPoints`'s bonus, gated by `HasStreakBonus`. The now-redundant external `calculateConsecutiveShot` call was removed from `BasketBallShotMadeCollision.cs`. **2026-08-14, addressed a `/code-review` finding**: this was flagged as shipped with no automated regression test, which AGENTS.md requires for scoring changes. Rather than fabricate a `GameRules`/`MatchRuntime` mock with no precedent in this suite, the two-pass score-and-update-streak sequence was extracted into `GameStats.ApplyMadeShot(BasketBallState, ShotScoringInput)` - a method that only touches `GameStats`/`BasketBallState`/`Level5.Core.ShotScoring`, none of which need a running match - and `Level5GameStatsApplyMadeShotTests.cs` now exercises it directly: one test makes four simulated consecutive threes and asserts the bonus lands on the 3rd (the threshold shot), not the 4th; another makes a two-pointer mid-streak and asserts it resets the count instead of extending it. Both assertions are exactly the two behaviors this row's bug broke. Verified by full compile + 404/404 EditMode (401 prior + 3 new) + 4/4 PlayMode + `validate-repository.ps1`; **still worth a manual Play Mode check of "In The Pocket" mode** since the extraction is now unit-tested but the scene-level wiring (`GameRules`/`MatchRuntime`/the marker list) is not. **2026-09-01, AUD-010 Phase 1c addendum**: `GameStats.ApplyMadeShot(BasketBallState, ShotScoringInput)` and `calculateConsecutiveShot(BasketBallState)` - the temporary seam this row's fix introduced so the AUD-065 ordering could be tested without a running match - are retired. The live made-shot path (`BasketBallShotMade.updateShotMadeBasketBallStats`) now captures `BasketBallState.TwoAttempt` itself and calls `MatchStats.ApplyMadeShot(bool, ShotScoringInput)` directly; `GameStats` has zero remaining `BasketBallState` references, enforced by `Level5GameStatsDependencyGuardTests`. `Level5GameStatsApplyMadeShotTests` is deleted - its three cases were exact duplicates of ones already in `Level5MatchStatsTests`, which is now this behavior's sole owner. |
| AUD-066 | `RacingVehicleController.Flip()` dereferences `damageDisplayObject` with no null guard | Medium | Closed | AUD-053 already fixed the same class's `Start()` (`SceneObjects.Find` for `player_damage_display_text`, which can legitimately return null) but never touched `Flip()`, which still did `damageDisplayObject.transform.localScale` unconditionally. The sibling human/CPU controllers (`PlayerController.Flip()`, `AutoPlayerController.Flip()`) both guard the identical block with `damageDisplayObject != null`. | `Flip()` runs on every horizontal direction change, so this throws a `NullReferenceException` on nearly every turn in any racing-minigame scene missing that object while enemies/enemies-only/sniper mode is active - the same "the twin got the fix, this one didn't" pattern AUD-035/049/051/052/053 already found repeatedly, just not previously checked in `player_racing`. | Fixed: added the same `damageDisplayObject != null &&` guard used by `PlayerController`/`AutoPlayerController`. |
| AUD-067 | `PlayerDunk.Start()` unguarded `GameObject.Find` | Low | Closed | `dunkPositionLeft = GameObject.Find("dunk_position_left").transform.position;` (and the `dunk_position_right` line beside it) throws immediately if either scene object is missing. Neither name is in `GameRules.RequiredSceneObjectNames`, the only list `Level5ProjectValidator` checks at build time, so nothing catches a missing marker before runtime - the same unguarded-`Find` shape AUD-028/047/052/053 already fixed elsewhere, just not previously checked in the dunk system. | `Start()` throws for the whole component if a scene is missing either dunk marker, with no build-time warning. | Fixed: both lookups now go through `SceneObjects.Find(name, this)` (the existing helper, used elsewhere for exactly this pattern), which logs the missing name and returns null instead of throwing. **2026-08-14, addressed a `/code-review` finding**: the first version of this fix left `dunkPosition*` at its serialized `Vector3.zero` default when a lookup failed, which is worse than the original crash - `Launch()`'s trajectory formula divides by `(H - R * tanAlpha)` and takes its `Sqrt`, so a degenerate zero target can produce `NaN` velocity or fling the player toward the world origin instead of failing loudly. Fixed by disabling `playerCanDunk` instead (the same flag `PlayerController` already checks before ever triggering a dunk), so a missing marker now silently removes the dunk capability rather than letting `Launch()` run against a position that was never real. No behavior change when both markers are present. |
| AUD-069 | `SniperCameraController` dereferences `SniperManager.instance` unguarded | Low | Closed | `Update()`'s gamepad-button handler called `SniperManager.instance.StartSniperBullet(...)` with no null check. `SniperManager.OnDestroy()` explicitly clears its own static to null on teardown (its own doc comment says callers must guard), and AUD-062 already fixed this same file's other unguarded singleton-style access (`Gamepad.current`) without touching this one. | A gamepad south-button press while the sniper camera is active but `SniperManager.instance` is transiently null (e.g. scene-transition timing) throws. Already known to be barely exercised - AUD-062's own verification notes this path "cannot be exercised without a controller, and its scene is not in the build" - so this is a real gap in code that gets essentially no automated or manual coverage. | Fixed: added `SniperManager.instance != null` to the existing condition, matching the guard pattern the class's own `OnDestroy` doc comment asks callers to use. |
| AUD-070 | Goal camera active-state can desync between `CameraManager` and `cameraUpdater` | Medium | Closed | `CameraManager.switchCamera()` unconditionally force-disables the goal camera (`Cameras[i].SetActive(false)` whenever `Cameras[i].name.Contains("goal")`) every time it runs, without telling `cameraUpdater`, which independently tracks the same camera's on/off state in its own `onGoalCameraEnabled` bool and toggles it based on player-to-rim distance in `Update()`. | If a player switches cameras (`switchCamera()`) while `cameraUpdater.onGoalCameraEnabled` is `true`, the goal camera gets force-disabled but the bool stays stale-true. On the next frame where the player is within the near-rim distance band, `cameraUpdater` reads the stale `true`, thinks the goal camera is already on, and calls `toggleCameraOnGoal()` to turn it *off* (a harmless no-op) while flipping its own tracked state to `false` - after which the far-from-rim branch can then turn the goal camera back *on* while the player is far from the rim, inverted from the feature's intent. | **2026-08-14: fixed**, without touching `CameraManager.cs` - `cameraUpdater` didn't have (and this pass avoided adding) a static `instance`, and `CameraManager` never held a live reference to it, so reaching across from `CameraManager` would have meant guessing whether `cameraUpdater` is genuinely single-instance (multiple camera "flavor" components could plausibly exist, one per camera in `CameraManager.Cameras[]`, with only the active one's `Update()` actually running). Instead, `cameraUpdater.Update()` now re-syncs `onGoalCameraEnabled` to the goal camera's real `activeSelf` at the top of every frame, self-healing exactly the way the adjacent `CameraOnGoalAllowed` check already did for a different desync case - no cross-class coupling added. Verified by full compile + 401/401 EditMode + 4/4 PlayMode + `validate-repository.ps1`; **still worth a manual Play Mode check** (switch cameras near the rim, observe goal-camera behavior) since this is presentation/camera-feel logic with no existing automated coverage. |
| AUD-071 | `PlayerHealthBar.Start()` has five unguarded scene/child lookups behind one mode gate | Low | Closed | Behind a single `if (EnemiesEnabled \|\| SniperEnabled \|\| EnemiesOnly \|\| ObstaclesEnabled \|\| IsBattleRoyal)` gate, `Start()` does `transform.Find("health_bar")`, `transform.Find("block_bar")`, `transform.Find("special_bar")`, `GameObject.Find(characterNameName)`, and `GameObject.Find(healthSliderValueName)`, each immediately `.GetComponent<...>()`'d with no null check, then `characterNameText.text = ...` immediately after. | Same class of bug as AUD-066/067/028/047/052/053, but wider: fixing only the two `GameObject.Find` calls the way AUD-067 fixed `PlayerDunk` would leave `characterNameText.text = ...` (line 49) dereferencing a possibly-null result, and would leave the three `transform.Find` calls just as unguarded as before - a partial fix here would be worse than no fix, since it would look complete without being complete. | **2026-08-14: fixed**, guarding all five and their downstream users in one pass rather than patching a subset. All five lookups resolve first; if any is null, or the component `GetComponent` returns from one is null, the whole thing logs one error and calls `gameObject.SetActive(false)` - the same graceful-degradation the mode-gate's `else` branch already used - **before** subscribing to `playerHealth`'s change events or writing `characterNameText.text`, so `setHealthSliderValue()`/`setBlockSliderValue()`/`setSpecialSliderValue()` (which still dereference these fields unconditionally, unchanged) can never run against a partially-null state. `instance` is now assigned only after successful resolution, so nothing can read a half-initialized `PlayerHealthBar.instance` during the failure window either. |
| AUD-072 | `StatsTableAllTime.Awake()` has eleven unguarded `GameObject.Find(...).transform.GetChild(1)` chains | Low | Closed | All eleven of the All Time Stats screen's value-text lookups chained `GameObject.Find(name).transform.GetChild(1).GetComponent<Text>()` directly, with no null check on the found object, the child index, or the component - not even the double-`Find` idiom used elsewhere in this codebase. | Unlike core per-shot HUD elements (`RangeMeter`, `BasketBallShotMarker` - checked and left alone; they run on every single shot in every basketball mode, so if their named objects were ever actually missing the game would already be crashing constantly), the All Time Stats screen is a single, narrow, infrequently-visited menu - exactly the kind of rarely-exercised path where a renamed or restructured scene object could sit broken without being noticed. | Fixed: all eleven now go through a shared `ResolveStatText` helper (`SceneObjects.Find` plus a child-count check before indexing), and `Awake()` deactivates the whole screen with one logged error if any of the eleven failed to resolve, before `Start()`'s `loadAllTimeStats()` (which still dereferences all eleven unconditionally) can run against a partial result. |
| AUD-073 | Three `TouchInput*ScreenController` classes checked a different object than the one they dereferenced | Low | Closed | `TouchInputProgressionScreenController`, `TouchInputStartScreenController`, and `TouchInputStatsScreenController` each guarded their touch-UI setup on `FindAnyObjectByType<TManager>() != null` (a component check) but then called `GameObject.Find("progressionScreen"\|"startScreen"\|"stats_manager")` twice, unguarded, immediately after - the exact "checked a different marker than the one used" shape AUD-053/066 already found elsewhere, just not previously checked in the touch-input (mobile) code paths. | If the manager component exists but the differently-named screen `GameObject` it looks up doesn't (e.g. a rename that touched one but not the other), `GetComponentInChildren<GraphicRaycaster>()`/`<EventSystem>()` throws on a touch-input device, in code with no automated coverage anywhere in this project. | Fixed: all three now resolve the screen object once via `SceneObjects.Find`, reuse the result for both `GetComponentInChildren` calls, and fall through to the existing `gameObject.SetActive(false)` else-branch when it's missing - same behavior when everything is present, no crash when it isn't. |
| AUD-074 | `EnemyController`/`EnemyHealthBar` null-safety gaps around a struck-by-lightning enemy with no health/health-bar wiring | Medium | Closed | Three related gaps found in the same 2026-08-15 sweep, two by a `/code-review` pass after the first fix landed. (1) `EnemyController.struckByLighning(int)` called `enemyHealthBar.DisplayCustomMessageOnDamageDisplay(...)` with no null check, even though `enemyHealthBar` is explicitly nullable elsewhere in the same class (the custom-camera rotation block already guards it) and every call site in the sibling `EnemyCollisions.cs` guards the identical call on `enemyHealthBar != null`. (2) `/code-review` found the line immediately above the new guard, `!enemyHealth.IsDead && enemyHealth.ApplyDamage(...)`, dereferenced `enemyHealth` just as unconditionally - the same class already treats `enemyHealth` as nullable via `enemyHealth?.ResetForSpawn()` and `CanAct => ... (enemyHealth == null \|\| !enemyHealth.IsDead)`. (3) `EnemyHealthBar.Start()` unconditionally dereferenced `transform.parent.GetComponentInChildren<EnemyHealth>()` and `GetComponentInChildren<Slider>()` (`healthSlider.maxValue = enemyHealth.MaxHealth`) with no null checks and no guard on `transform.parent` itself - the same shape AUD-071 already fixed for `PlayerHealthBar`. | `BehaviorNpcRob.LightningStrike()` calls `struckByLighning` for every tagged `enemy` in the scene, checking only that an `EnemyController` is present - not that an `EnemyHealth` or `EnemyHealthBar` is. Either missing dependency threw an uncaught `NullReferenceException` that aborted the coroutine before `stateKnockDown`, `FreezeEnemyPosition()`, the camera flash, the lightning animation, or the death/knockdown resolution ever ran; for the `enemyHealth` gap specifically, the crash happened before damage was even applied, since it's the very first line of the method. Separately, a partially-wired `EnemyHealthBar` (present but missing its `EnemyHealth` sibling, its `Slider` child, or attached with no parent) would NRE in `Start()` before ever subscribing to `OnHealthChanged`, in code with no automated coverage. | Fixed: (1)/(2) both dereferences in `struckByLighning` are now guarded - `enemyHealthBar` the same way `EnemyCollisions` already does, `enemyHealth` the same way `CanAct` already does (`enemyHealth != null && !enemyHealth.IsDead && ...`, so a missing `EnemyHealth` is treated as "can't be killed by this hit" rather than crashing) - the rest of the method (knockdown, camera flash, animation, death resolution) still runs unconditionally in both cases. (3) mirrored the AUD-071 pattern: both lookups (plus a `transform.parent != null` guard) resolve before either is used; if either is missing, one logged error names which one(s), then `gameObject.SetActive(false)` before `OnHealthChanged` is subscribed or any UI is touched. No behavior change when everything is present. |
| AUD-075 | `BehaviorVehicleLawnmower.Start()` has an unguarded `transform.Find("sprite")` and an unguarded `returnPositions[0]` index | Low | Closed | `currentSprite = transform.Find("sprite").GetComponent<SpriteRenderer>();` (and the identical `anim = transform.Find("sprite").GetComponent<Animator>();` beside it) throws if the object has no `sprite` child; `currentTarget = returnPositions[currentTargetIndex].transform.position;` throws if `GameObject.FindGameObjectsWithTag("vehicle_position_marker")` returns an empty array - the same unguarded-lookup shape AUD-028/047/052/053/066/067/074 already fixed elsewhere. The component (`Assets/Resources/Prefabs/characters/npc_auto/npc_staff_auto.prefab`) is not currently referenced by GUID from any scene or other prefab and not loaded by path from any script, so neither throw is reachable as the repository stands today - fixed anyway on explicit instruction rather than left as a documented-only gap, since the guard is cheap and correct regardless of current reachability. | None currently (unreachable), but if this prefab is ever spawned - the class exists specifically to drive an NPC vehicle - either gap would throw in `Start()` and abort the whole component, the same way the already-fixed sibling cases did while they were live. | Fixed: both lookups resolve first (`transform.Find("sprite")` once, reused for both `SpriteRenderer` and `Animator`); if it's missing or `returnPositions` is empty, one logged error names which, then `enabled = false` before any of `Update()`'s fields (which still dereference these unconditionally, unchanged) can run against a partial result. No behavior change when both are present. |
| AUD-076 | `DBHelper.UpdatePlayerProfileProgression` and `DBHelper.getAllTimeStats` never acquire the class's cooperative `databaseLocked` flag | Medium | Closed | Every other method in `DBHelper` (~20 of them) sets `databaseLocked = true` before opening its own `SqliteConnection` and resets it on every exit path; these two opened a connection and ran a query/`UPDATE` with no lock at all, while both still reset `DatabaseLocked = false` in their `catch` block - evidence the acquire line was dropped in each, not that the lock was intentionally skipped. Grepping every caller found neither has a live caller anywhere in `Assets/Scripts` or any scene/prefab: `UpdatePlayerProfileProgression`'s only wrapper is `DBConnector.savePlayerProfileProgression` (unused) - progression writes now go through the ledger-based `ApplyProgressionResult`/`ProgressionService.ApplyMatchResult` path from AUD-011, which does hold the lock correctly; `getAllTimeStats` (unused, its per-field siblings `getFloatValueAllTimeFromTableByField`/`getIntValueAllTimeFromTableByField` are the live path `StatsTableAllTime` actually uses). Both look like pre-AUD-011/pre-AUD-072 write/read paths, superseded but never removed (the same "written but never called" shape as `CharacterProgressMigration`, which AUD-011 deleted outright). | None currently (unreachable from any live path), but if either method is ever called concurrently with another locked DB call, it would not wait for `!databaseLocked` the way `InsertCharacterProfile`/`InsertCheerleaderProfile`/`InsertUserCoroutine` do, and would not be waited for by them either - a real gap in the cooperative-lock contract this class otherwise enforces everywhere, on methods that touch character progression and all-time stats. | Fixed rather than deleted (fixing was the instruction given; deleting either superseded method remains a separate, bigger call for later if confirmed safe to remove). `UpdatePlayerProfileProgression` now matches the synchronous, bool-returning lock shape `EnsureCharacterProfilesForAccount`/`HasCharacterProfilesForAccount` already use: returns `false` immediately if `databaseLocked` is already true, otherwise sets it before the `try` and releases it in a `finally`. `getAllTimeStats` sets `databaseLocked = true` before its `try` (matching its plain-getter siblings, which don't check-first) and releases it in a `finally`. No behavior change to either method's SQL or return values. |
| AUD-077 | `AnaylticsManager.PointsScoredEnemiesDisabled` divides by `ShotAttempt` with no zero-guard | Low | Closed | `(basketBallStats.ShotMade / basketBallStats.ShotAttempt).ToString("##.####")` - both operands are `int`, so a zero `ShotAttempt` throws `DivideByZeroException` at runtime (unlike float division, which would just produce `NaN`). The sibling method directly above it, `PointsScoredEnemiesEnabled`, computes the identical ratio but already guards it with `if (basketBallStats.ShotAttempt != 0)`, defaulting to `0` otherwise - the same "twin got the fix, this one didn't" shape AUD-035/049/051/052/053/066 already found repeatedly elsewhere. | A player who ends a session with zero shot attempts (e.g. quits immediately) would crash this analytics call. Grepped every caller of both `PointsScoredEnemies*` methods and found neither is called anywhere in `Assets/Scripts` today - unreachable from any live path, same as AUD-076's two methods - fixed anyway for the same reason: the guard is cheap, correct, and removes a landmine if analytics calls are ever wired back up. | Fixed: mirrors the sibling method's guard exactly - `accuracy` defaults to `0` and is only computed when `ShotAttempt != 0`. No behavior change when `ShotAttempt` is nonzero. |
| AUD-078 | Nine call sites dereferenced an `APIHelper` callback result with no null check | Medium | Closed | Every `APIHelper` method that returns data (`PostToken`, `UserNameExists`, `PostUser`, `GetUserByUserName`, `GetServerMessages`, `PostUnsubmittedHighscores`, `GetLatestBuildVersion`, `GetHighscoreCountByModeid`, `GetHighscoreByModeid`) funnels through `SendJson`, whose `completed?.Invoke(...)` call sits *after* its own `try`/`finally` block (`APIHelper.cs:522-531`) rather than inside it - so an exception thrown while building the request (header/encoding setup, not a network failure, which is already handled via `UnityWebRequest.Result`) would run the `finally` cleanup but skip the `completed` invocation entirely, leaving the caller's `result` variable `null` after the `yield return`. `UserAccountManager.LoginGuestCoroutine` already guards this with `result == null \|\| !result.Success` after exactly this call shape; nine other call sites across `LocalAccount.cs`, `ServerMessagesManager.cs`, `AccountManager.cs` (five sites), `StatsManager.cs` (two sites), `StartManager.cs`, and `APIConnector.cs` (two sites) did not - the same "one sibling got the guard, the others didn't" shape as the `GameObject.Find` rows above, just in the networking layer instead of scene lookups. | A malformed request (not a network failure - those already return a valid `Fail` result) at any of these nine sites would throw a `NullReferenceException` on `result.Success`/`.Value`/`.Error`, crashing account creation, login, username-check, server-message loading, build-version display, online high-score paging, and unsubmitted-score resubmission. Low probability (requires an exception inside request setup, not a connection/HTTP failure) but a real gap in code that talks to the account/persistence boundary, which AGENTS.md flags as high-risk. | Fixed: all nine now guard `result == null` (or the call's actual local variable name) before reading any of its members, matching `UserAccountManager.LoginGuestCoroutine`'s existing pattern - each falls back to the same messaging/behavior the method already used for an ordinary `Fail` result. No behavior change on any success or already-handled-failure path; `SendJson` itself was not changed, since restructuring its `try`/`finally` to guarantee invocation is a bigger, riskier change affecting every API call in the game, not a one-line guard at nine call sites. |
| AUD-079 | `RacingVehicleController.Start()` has an unguarded `transform.root.transform.Find("drop_shadow")` | Medium | Closed | `dropShadow = transform.root.transform.Find("drop_shadow").gameObject;` throws if the vehicle's root has no `drop_shadow` child - the same unguarded-lookup shape AUD-028/047/052/053/066/067/074/075 already fixed elsewhere, just not previously checked in this file. `dropShadow` is then dereferenced unconditionally every frame in `Update()` ("keep drop shadow on ground at all times"), in both the grounded and airborne branches. Confirmed live via the script's GUID, which is wired directly into `Assets/Scenes/minigame_racing.unity` (not just a prefab). | `Start()` throws for the whole component if the scene's vehicle object is ever restructured without its `drop_shadow` child, aborting all of `Start()`'s later setup (damage display wiring, sniper knockdown-time tuning) - not just the drop-shadow feature - and leaving `Update()` to NRE every frame afterward had `Start()` partially succeeded. | Fixed: the lookup resolves to a `Transform` first and logs a named error if missing, instead of chaining `.gameObject` directly onto a possibly-null result; the rest of `Start()` still runs either way. Both `Update()` call sites now guard `dropShadow != null` before touching it, the same pattern already used for `damageDisplayObject` in this same file's `Flip()` (AUD-066). No behavior change when the child is present. |
| AUD-080 | `SunglassesCollision` dereferences a serialized reference and a scene singleton with no guard | Low | Closed | `Start()` called `CameraPostProcessing.SetActive(false)` on a `[SerializeField] GameObject` nothing validates at build time; `OnTriggerEnter` called `TheyLiveManager.instance.TheyLiveEnabled = true` and `spriteRenderer.enabled = false` unconditionally, where `TheyLiveManager` is a scene-scoped singleton (confirmed live via GUID in `level_17_rumble_pit`, `level_18_aveb2`, `minigame_racing`, but not guaranteed present in every scene this component could be placed in) and `spriteRenderer` comes from an unguarded `GetComponentInChildren`. | `SunglassesCollision.cs` is not currently attached to any prefab or scene in the repository (grepped its GUID) - unreachable today, same as several other findings this session - but if placed on a pickup in a scene without a `TheyLiveManager`, or with `CameraPostProcessing` left unassigned, either gap throws and the exception propagates out of `OnTriggerEnter`/`Start()`. | Fixed anyway per explicit instruction to fix all findings regardless of current reachability: `CameraPostProcessing`, `TheyLiveManager.instance`, and `spriteRenderer` are now each guarded independently at their point of use, so a missing dependency degrades that one piece (no post-processing toggle, no They-Live-mode trigger, or the sprite stays visible) instead of throwing and skipping everything after it. No behavior change when all three are present. |
| AUD-081 | Five more files share the unguarded `transform.Find(...).member` shape AUD-079 fixed in `RacingVehicleController` | Medium-High | Closed | A systematic re-sweep of every remaining `GameObject.Find(...).member`/`transform.Find(...).member` call site in `Assets/Scripts` (prompted by AUD-079) found five more real instances of the exact same shape: **`PlayerController.Start()`** and **`AutoPlayerController.Start()`** (the human and CPU basketball-mode player controllers - the game's two most central, always-active scripts) both had `dropShadow = transform.root.transform.Find("drop_shadow").gameObject;` unguarded, then dereferenced `dropShadow` unconditionally every frame in `Update()`'s grounded/airborne branches - a near-identical copy of the exact bug AUD-079 had just fixed in `RacingVehicleController`, strongly suggesting a shared origin. **`AutoPlayerDefense.Start()`** had the same `dropShadow` gap (`transform.Find("drop_shadow")` this time, no `.root`), dereferenced unconditionally every frame in `Update()`. **`BehaviorPrimo.Start()`** had the exact `transform.Find("sprite").GetComponent<SpriteRenderer\|Animator>()` double-lookup AUD-075 fixed in `BehaviorVehicleLawnmower`, with `anim` then dereferenced unconditionally in `Update()`. **`BehaviorNpcAutonomous.Start()`** had the identical `anim = transform.Find("sprite").GetComponent<Animator>();` gap, positioned *before* that method's own already-fixed NPC-3 guard for a different lookup in the same method - so the already-fixed crash was preceded by an unfixed one. (`Timer.cs`, `CameraManager.cs`, and `BasketBall.cs`/`BasketBallAuto.cs`'s remaining matches were checked and left alone: `Timer.cs`'s are pre-guarded by an `if (...Find(...) != null)` check just not deduplicated into a single lookup; `CameraManager.cs`'s is the already-investigated pervasive `messageDisplay` pattern; `BasketBall.cs`/`BasketBallAuto.cs`'s are in the single most-exercised script in the entire game - every human and CPU shot, in every mode - so a genuine gap there would already be crashing constantly, the same reasoning the AUD-072/073 investigation already applied to `RangeMeter`/`BasketBallShotMarker`.) | `PlayerController`/`AutoPlayerController` are confirmed live for literally every human and CPU player in every basketball mode - a missing `drop_shadow` child would abort `Start()` before the rest of setup (shot meter, health, damage display) ran, and crash `Update()` every frame afterward on any partial success. `BehaviorNpcAutonomous` is confirmed live across nine NPC prefabs (`npc_baby_auto`, `npc_centaur_auto`, `npc_flash_auto`, `npc_ghost_auto`, `npc_harrycharles_auto`, `npc_mouse_droid_auto`, `npc_rabbit`, `npc_snake_auto`, `player_flash_auto_play`) plus `Flash.prefab`. `AutoPlayerDefense` and `BehaviorPrimo` were not found attached to any current prefab/scene (grepped by GUID) - unreachable today, fixed anyway per the same explicit instruction as AUD-080. | Fixed: `PlayerController`/`AutoPlayerController`/`AutoPlayerDefense` all resolve the `Transform` first, log a named error if missing, and guard both `Update()` dereferences with `dropShadow != null` - matching AUD-079's exact fix shape. `BehaviorPrimo`/`BehaviorNpcAutonomous` resolve `transform.Find("sprite")` once, log a named error and `enabled = false` if missing (matching AUD-075's `BehaviorVehicleLawnmower` fix), since both dereference `anim` unconditionally every frame in `Update()` with no cheaper partial-guard available. No behavior change when every lookup succeeds. |
| AUD-082 | `BodyGuardAnimationEvents.enemyController` is typed `EnemyController`, which no bodyguard object ever has | High | Closed | `BodyGuardAnimationEvents.cs` is a near-verbatim copy of `EnemyAnimationEvents.cs` (same SFX methods, same projectile-spawn logic, same attack-box handling) - but the copy never updated its controller field's type: `[SerializeField] EnemyController enemyController;`, resolved in `Start()` via `transform.parent.GetComponent<EnemyController>()`. `BodyGuardController` and `EnemyController` are two independent classes (both implement `ICombatAgent`, neither derives from the other), so this lookup can never succeed on an actual bodyguard, whose parent has a `BodyGuardController` instead - confirmed `BodyGuardController` exposes the identical members this file uses (`facingRight`, `stateAttack`, `RigidBody`, `FreezeEnemyPosition`/`UnFreezeEnemyPosition`), leaving no doubt this should have been the type all along. Confirmed live: the script's GUID is attached to `Assets/Resources/Prefabs/bodyguards/bodyguard_ian.prefab`. | `enemyController` stayed `null` for every bodyguard for the lifetime of the object. Two guarded call sites (`checkAttackBoxDisabledCorrectly`, `SetProjectileFacing`) silently degraded instead of crashing, but `applyForceToDirectionFacingXAndY(float)` dereferences `enemyController.UnFreezeEnemyPosition()` and `.facingRight`/`.RigidBody` completely unguarded - any bodyguard animation event that calls it (a knockback/recoil force, by the method's name) threw a `NullReferenceException` for every bodyguard in the game, always, not intermittently. | Fixed: the field and both `GetComponent`/lookup call sites are retyped to `BodyGuardController`, the actual component every bodyguard's parent carries. No other member usage in the file needed to change, since `BodyGuardController` already exposes every member this file reads under the same names. |
| AUD-083 | Every `cpu_players/*.prefab` had `PlayerCollisions` instead of `AutoPlayerCollisions` on its hitbox object | Critical | Closed | Resolves the deferred uncertainty AUD-001 flagged in 2026-08-13: all 22 `Assets/Resources/Prefabs/characters/cpu_players/*.prefab` files (every CPU opponent character used across the game's normal modes) have the `PlayerCollisions` MonoBehaviour on their "hitbox" child object, but that object is tagged `autoPlayerHitbox` - confirmed by direct YAML inspection and cross-checked against the equivalent human prefab (`player_ak47.prefab`), whose identical component sits on a "hitbox" object correctly tagged `playerHitbox`. `PlayerCollisions.OnTriggerEnter`'s very first condition is `gameObject.CompareTag("playerHitbox")`, so on every CPU prefab it never once evaluates true. `AutoPlayerCollisions.cs` - the component actually written to check `gameObject.CompareTag("autoPlayerHitbox")` - exists in the codebase and has identical serialized fields (`playerIdentifier`, `playerController`, `autoPlayerController`, `playerHealth`, `playerCanBeKnockedDown`, in the same order), but as of this finding was attached to exactly one prefab in the entire project: `cpu_players_defense/cpu_player_defense_oldreal.prefab`. | Every CPU opponent in `cpu_players/*` (used in the game's standard modes, not just the defense-specific roster) had **no working hit detection at all** - `OnTriggerEnter` on the hitbox silently never ran, so CPU players could never take damage from enemy attacks, obstacles, traffic, disintegrate attacks, or sniper fire in any mode with those enabled, and could never be knocked down despite `playerCanBeKnockedDown: 1` being explicitly authored on the (dead) component. A combat-integrity gap affecting every non-defense CPU character in the roster, not a rare edge case. | User confirmed via the recommended option before any prefab was touched, given the scale (22 files) and gameplay-balance impact. Fixed by swapping the `m_Script` GUID on each prefab's existing `PlayerCollisions` MonoBehaviour block from `PlayerCollisions` to `AutoPlayerCollisions` - the serialized field schema is identical between the two components, so no field data needed to change, only which script the block binds to (verified against `cpu_player_defense_oldreal.prefab`'s own correctly-wired block as the template). Minimal, single-line diff per prefab. Verified via full compile + EditMode (408/408) + PlayMode (4/4) + `validate-repository.ps1`; **this is a serialized-data change with real gameplay-balance impact and no automated coverage for the collision behavior itself - needs a manual Play Mode check** (enemies/traffic/obstacles/sniper modes, confirm CPU opponents now take damage and can be knocked down) before shipping. |
| AUD-084 | Five `DBHelper` reads gated their query behind a redundant `isTableEmpty(...)` pre-check | Low | Closed | `getAllTimeStats`, `getCheerleaderProfileStats`, `getUserProfileStats`, `localUserExists`, and `getUnsubmittedHighScoreFromDatabase` each wrapped their real `SELECT` in `if (!isTableEmpty(tableName)) { ... }`. `isTableEmpty` opens its own second `SqliteConnection` and runs its own `SELECT count(*)` query before the real one runs - doubling the connection/query count on all five paths for a check the real query doesn't need: a `SELECT` against an empty table already returns zero rows on its own, so the reader's `while (reader.Read())` loop simply never executes. `LoadManager.cs` also called `isTableEmpty("CheerleaderProfile")` with a raw string literal instead of `Constants.LOCAL_DATABASE_tableName_cheerleaderProfile`, the same literal-vs-constant drift risk as AUD-086. | Pure overhead, not a correctness bug - twice the connection/query cost on five read paths (all-time stats, cheerleader unlocks, user list, login-existence check, unsubmitted-score resubmission) for no behavioral benefit. | Fixed: all five pre-checks removed: the guarded query runs directly and produces the identical result (empty list/zero count) when the table is empty. `isTableEmpty` itself was kept - it has two legitimate, non-redundant callers (`UserAccountManager.cs`, `LoadManager.cs`) that use it for real flow-control decisions, not as a belt-and-suspenders guard - and its own literal-string caller in `LoadManager.cs` was switched to the named constant. No behavior change on a non-empty table; on an empty one, both call counts and reader iteration counts are unchanged (zero either way). |
| AUD-085 | Every database method opened and disposed its own `SqliteConnection` per call, and `DBConnector`/`DBHelper` each built an independent connection string | Medium | Closed | Roughly 40 call sites across `DBHelper.cs` (~30) and `DBConnector.cs` (~7) each did `using (IDbConnection dbconn = new SqliteConnection(connection)) { dbconn.Open(); ... }` - a fresh native SQLite connection object opened and torn down on every single database call, including ones fired every few seconds during normal play (score/stats saves). There was never any concurrency for the per-call pattern to isolate: every caller already serializes through the cooperative `databaseLocked` flag (`docs/persistence-boundaries.md` already documented this), so per-call connections bought isolation nothing needed while paying real setup/teardown cost on every call. Separately, `DBConnector` built its own connection string independently of `DBHelper`'s (`"URI=file:" + path` vs `"Data source=" + path`) - two representations of the same file path with no shared source of truth. | Overhead on every database interaction in the game, and a latent drift risk from the duplicated connection-string construction (harmless today since both formats are valid and point at the same file, but a rename or path change made in one place and not the other would silently create two different databases). | Fixed: `DBHelper` now owns one lazily-opened, defensively-reopened `SqliteConnection` (`internal SqliteConnection Connection`) kept for the component's lifetime and disposed in `OnDestroy`; every one of its own ~30 methods was migrated to it, verified individually (not a blind file-wide rewrite) - `using` wrappers around the connection itself were removed (a `using` would have disposed the *shared* connection at the end of the first call), five methods that previously did `var dbconn = new SqliteConnection(connection); using (dbconn) { ...; dbconn.Close(); }` had both the wrapper and the explicit `Close()` removed the same way. `DBConnector`, which already holds a `DBHelper` reference on the same GameObject, was migrated to reuse `dbHelper.Connection` instead of building its own connection string and object - the format-drift risk is gone because there is now exactly one connection string and one connection instance for the whole app. Verified via full compile + EditMode (408/408) + PlayMode (4/4) + `validate-repository.ps1` after each migration stage. No behavior change on any query's SQL, parameters, or results. **2026-08-17, addressed a `/code-review` pass on this diff**: two real gaps found. (1) `OnDestroy()`'s new `sharedConnection.Close()`/`Dispose()` had no surrounding `try`/`catch` - unlike every per-call connection before it, which was always torn down inside its own method's `try`/`catch`, this teardown ran completely outside any of that, so a broken native handle at destruction time (e.g. the `persistentDataPath` volume went away) would have been an uncaught exception during object destruction, a failure mode that could not occur under the old per-call design. Fixed with its own `try`/`catch`/`finally`. (2) moving every method onto one shared connection/transaction raises the cost of the pre-existing inconsistency in which methods check `if (databaseLocked)` before acquiring it - `EnsureCharacterProfilesForAccount`/`HasCharacterProfilesForAccount`/a few progression methods already did; roughly 22 others set `databaseLocked = true` unconditionally. Under the old design, two overlapping calls only risked an isolated `SQLITE_BUSY` on separate connection objects; sharing one live connection/transaction raises the failure mode to shared-state corruption if that invariant is ever violated by a future change (nothing here currently violates it - every caller already awaits the lock - so this was defense-in-depth, not a live bug). Fixed: all 22 methods now check-and-reject the same way the two already-safe ones do; the 3 coroutine methods were already safe via `yield return new WaitUntil(() => !databaseLocked)` before acquiring, so were left as-is. Also fixed a third finding in `LoadManager.cs`: the `CharacterProfile` sibling of the `CheerleaderProfile` literal-vs-constant fix (AUD-084) had been missed in the same method. Re-verified via full compile + EditMode (408/408) + PlayMode (4/4) + `validate-repository.ps1`. **2026-08-17, second `/code-review` pass, three more findings.** (1) `DBConnector.tableExists()` runs on the shared connection but never checked `DatabaseLocked` - unlike every other consumer of that connection, so it could in principle run while another method held an open transaction on the same live connection object. Fixed not by adding a check-and-reject guard on `tableExists()` itself (a "busy" `false` would be indistinguishable from a genuine "table doesn't exist" to its caller and could trigger unwanted re-seeding, a worse outcome than the caller waiting) but by having `LoadManager`'s coroutine - the only caller - `yield return new WaitUntil(() => !DBHelper.instance.DatabaseLocked)` once before the whole synchronous block that calls it, since nothing yields inside that block once entered. `VerifyDatabase()` was flagged alongside it but is already safe as-is - its only caller, `createDatabase()`, already holds the lock for the whole call chain, so a naive guard on `VerifyDatabase()` itself would see its own caller's lock and incorrectly refuse to run; documented with a comment instead of changed. (2) `Connection`'s reopen check (`if (state != Open) Open()`) treated a `Broken` connection (a faulted native handle, e.g. from a mid-transaction failure) the same as a cleanly `Closed` one and tried to reopen the same object in place - not a reliable recovery for a broken handle. Fixed: a `Broken` connection is now disposed and replaced with a fresh instance before the open/reuse logic runs. (3) the `if (databaseLocked) { return X; }` guard added to ~22 methods for the prior round is duplicated per-method rather than centralized, which is the same *shape* of duplication (not the same bug) that let AUD-076's two methods lose their lock release unnoticed. Not fixed: centralizing lock acquisition across methods with genuinely different return types (`bool`, `void`, `int`, `float`, `List<T>`, `GameStats`) needs either generics with a per-call default or a disposable-guard pattern, either of which is a real design decision touching all 22 call sites again - noted as a `LATER` follow-up rather than forced into this already-large diff; the current duplication is a simple, visually-uniform 3-line block at the top of each method (grep-verifiable, as this session did), a materially easier pattern to keep correct than AUD-076's shape (a missing line buried inside a 30-60 line method body). Re-verified via full compile + EditMode (408/408) + PlayMode (4/4) + `validate-repository.ps1`. |
| AUD-086 | `HighScores`/`AllTimeStats`/`CharacterProfile`/`User` `CREATE TABLE` statements were each defined twice, with one pair already drifted | Medium | Closed | `DBConnector.createDatabase()` (the method that actually runs first on every install, via `Start()`) defined all four tables inline in one large SQL string. Three of the four were *also* defined a second time in standalone `createTableUser()`/`createTableAllTimeStats()`/`CreateTableCharacterProfile()` methods - and the `CharacterProfile` pair had already drifted: the standalone version added `playerName TEXT NOT NULL, objectName TEXT NOT NULL` that the inline version never had. Because the inline version always runs first, that stricter version's own `CREATE TABLE IF NOT EXISTS` was a permanent no-op on every install - dead intent, not dead code. Grepping every call site found `createTableUser()` and `createTableAllTimeStats()` were never called at all. Table names were also raw string literals throughout `DBConnector.cs` (`"HighScores"`, `"CharacterProfile"`, etc.) rather than the `Constants.LOCAL_DATABASE_tableName_*` constants `DBHelper.cs` uses consistently, and `createDatabase()`'s SQL was wrapped in a `String.Format(...)` call with zero format placeholders - a vestigial no-op wrapper. | The schema drift was latent, not live (the stricter constraint never actually took effect), but the duplication itself was a real maintenance hazard - a future schema change applied to one copy and not the other would silently reintroduce drift, and the raw literals meant a renamed `Constants` value wouldn't be caught by the compiler in the `CREATE TABLE` statements. | Fixed: each table's `CREATE TABLE` SQL now exists in exactly one place (a `private static readonly string ...CreateTableSql` field, built from `Constants.LOCAL_DATABASE_tableName_*` via string interpolation, not a literal), standardized on the constraint that was actually live (no `NOT NULL`, since nothing had ever enforced the stricter one and adding it now could reject data other code doesn't expect to be rejected). `createDatabase()`, `CreateTableCharacterProfile()`, and `CreateTableCheerleaderProfile()` all reference the shared constants instead of duplicating SQL text; `createTableUser()` and `createTableAllTimeStats()` were deleted outright (confirmed zero callers). The `UPDATE User SET password/bearerToken = NULL` scrub queries and the `DROP TABLE` statements in `dropDatabase()` were switched to the named constants too. The vestigial `String.Format` wrapper was removed. No schema or behavior change for any existing database - the effective schema (the one that was actually live) is unchanged, just now defined once. |
| AUD-087 | Every `DBHelper` method released its lock via duplicated code at each return point instead of `finally` | Low | Closed | Two real bugs earlier this session (AUD-076) were exactly this shape: a method that forgot to reset `databaseLocked` on one of its exit paths, because the reset was hand-copied at every `return` and in the `catch` block instead of centralized. The other ~30 methods in `DBHelper.cs` weren't missing a reset (each one already had it duplicated at every exit point, so the lock was always released in practice), but the duplication was exactly the shape that let AUD-076's two methods lose theirs unnoticed. `docs/persistence-boundaries.md` already flagged this as "looks unbalanced at a glance." | Not a live bug in the ~30 methods that already had every exit path covered, but the inconsistent style is what let two other methods silently lose an exit path. Leaving it inconsistent kept the same class of bug available to reintroduce the next time a method gained a new early return. | Fixed: every method that acquires `databaseLocked` now releases it in a single `finally` block, and every hand-duplicated release at a `return` or in a `catch` was removed as redundant. Verified each conversion individually against its method's actual exit paths (some methods have an early return before the main query, some have exit paths inside nested try-adjacent branches) rather than a blind pattern substitution across the whole file. No behavior change: a `finally` releases on exactly the same paths the duplicated code already covered, just once instead of two-to-three times per method. |

### Note on the AUD-012 assembly split (2026-08-07)

The folder separation and its enforcement are done. The remaining half - giving `Assets/Scripts` its
own `.asmdef` - is **blocked on a Unity session**, not on effort, and should not be attempted blind.

What was established:

- **No reverse dependency.** Nothing under `Assets/Joystick Pack`, `Assets/Standard Assets`,
  `Assets/OmniSARTechnologies`, or `Assets/DialogueManager.cs` references any type in
  `Assets/Scripts`. The split is therefore possible without a circular assembly reference.
- **The outbound surface is only two types.** `Joystick`/`FloatingJoystick` (used by
  `GameLevelManager` and `RacingGameManager`) and `DialogueManager` (used by `LocalAccount` and
  `UserAccountManager`). `LiteFPSCounter` looked like a third but is only a `GameObject.Find`
  string, not a type reference. So the split needs one new asmdef for the Joystick Pack and a home
  for `DialogueManager` - not a wholesale reorganisation.
- **What blocks it.** An asmdef must name every package assembly it uses, and a wrong name is a
  project-wide compile failure. `Assets/Scripts` pulls in `UnityEngine.InputSystem`,
  `Unity.Mathematics`, `Newtonsoft.Json`, `UnityEngine.Analytics`, `Mono.Data.Sqlite`, and
  `Unity.IO.LowLevel.Unsafe`. Some of those resolve to precompiled DLLs (referenced automatically);
  others are asmdef-based package assemblies that must be listed explicitly. Which is which cannot
  be determined from the repository - it depends on the resolved package versions in `Library/`,
  which is correctly not tracked in git.

Do this from an open Unity editor, where the console names any assembly that fails to resolve.

One related observation worth recording: `ProjectSettings/EditorSettings.asset` and every `.unity`
scene are currently **binary** on disk, even though `Level5ProjectValidator.ConfigureSourceControlPolicy`
sets `SerializationMode.ForceText` on load. That confirms the project has not been opened in Unity
since that policy was added. The first open will convert them - which also makes AUD-046's
uninspectable serialized value greppable for the first time.

## Finding Details

### AUD-001: Combat State Ownership Is Split

Evidence:

- `PlayerCollisions` applies damage, toggles player controller damage state, displays health UI messages, and triggers audio.
- `PlayerHealth` owns health values and death events.
- `PlayerController` watches `TakeDamage` in `Update` and starts `PlayerTakeDamage`.
- `PlayerAnimationEvents`, `EnemyAnimationEvents`, and `BodyGuardAnimationEvents` enable/disable attack boxes from animation callbacks.
- `PlayerAttackQueue` coordinates enemy/bodyguard reservations and also writes back into detection components.

Why it matters: there is no single combat transaction. A hit can touch collision logic, health, animation, UI, audio, queue state, stats, and AI behavior. That makes it hard to guarantee idempotent death, clean attack interruption, or reliable UI updates.

Recommended next move: introduce a small combat contract layer before further behavior changes: `DamageInfo`, `IDamageable`, `ICombatAgent`, `HealthChanged`, `Died`, and `AttackDefinition`.

### AUD-002: PlayerController Is Over-Scoped

Evidence:

- `PlayerController` resolves UI with `GameObject.Find` at `Assets/Scripts/player/PlayerController.cs:171`.
- It pulls health and rim state from `GameLevelManager.instance` at `Assets/Scripts/player/PlayerController.cs:178`.
- It reads mobile joystick input from `GameLevelManager.instance` in movement at `Assets/Scripts/player/PlayerController.cs:243`.
- It owns attack entry with `PlayerAttack` at `Assets/Scripts/player/PlayerController.cs:658`.
- It owns damage reaction coroutine state with `PlayerTakeDamage` at `Assets/Scripts/player/PlayerController.cs:779`.
- It triggers sniper behavior through `SniperManager.instance` around `Assets/Scripts/player/PlayerController.cs:573`.
- It mutates camera FOV through `CameraManager.instance` around `Assets/Scripts/player/PlayerController.cs:881`.

Why it matters: this class is acting as movement controller, input adapter, combat state machine, basketball action owner, animation bridge, UI message writer, sniper trigger, and camera modifier. Any refactor risks side effects across unrelated gameplay features.

Recommended next move: do not split it all at once. Start by extracting the lowest-risk seam first: input intent collection or damage reaction state. Keep a facade on `PlayerController` while callers migrate.

### AUD-003: Actor Health Is Inconsistent

Evidence:

- `PlayerHealth` clamps values, exposes `OnHealthChanged`, `OnBlockChanged`, `OnSpecialChanged`, and `OnDied`, and rejects damage after death.
- `EnemyHealth` and `BodyGuardHealth` expose settable `Health`/`IsDead` properties without the same event or clamping model.
- Enemy/bodyguard damage and death cleanup are driven from collision/controller scripts instead of a reusable health lifecycle.

Why it matters: every actor type can drift in how death is detected, whether repeat damage is ignored, how UI updates, and who performs cleanup.

Recommended next move: create a reusable `HealthComponent` behavior or base service with optional actor adapters. Migrate enemy or bodyguard first because player health has the most existing UI dependencies.

### AUD-004: Attack Queue Is Still Actor-Specific

Evidence:

- `PlayerAttackQueue` stores attackers and bodyguards as `GameObject` lists.
- It discovers slots and bodyguards through tags at `Assets/Scripts/player/PlayerAttackQueue.cs:81` and `Assets/Scripts/player/PlayerAttackQueue.cs:97`.
- It writes directly into `EnemyDetection` and `BodyGuardDetection` state when assigning or clearing reservations.
- `EnemyDetection` gets the queue through `GameLevelManager.instance.PlayerController1.PlayerAttackQueue`.
- `EnemyController` and `BodyGuardController` read queue internals like `BodyGuards`, `EnemiesQueued`, and attack positions directly.

Why it matters: the reservation concept is good, but the queue currently knows too much about the actor implementations. New melee actors will require queue changes, and queue invariants can be bypassed through public lists.

Recommended next move: introduce `CombatReservation` and `ICombatAgent`. The queue should return a reservation object and expose read-only slot state. Detection/controller scripts should not mutate queue internals directly.

### AUD-005: Global Managers and Scene Searches Are Load-Bearing Dependencies

Evidence:

- Common runtime scripts use `GameLevelManager.instance`, `BasketBall.instance`, `GameRules.instance`, `Pause.instance`, `SFXBB.instance`, and other singletons.
- Scene object lookup appears in gameplay paths such as `PlayerController`, `EnemySpawner`, `TrafficManager`, `Timer`, `BasketBall`, `DBHelper`, and `DevFunctions`.
- Some systems search by object name or tag for required objects like `messageDisplay`, `shot_clock`, `steelCageRootObject`, spawn markers, and queue positions.

Why it matters: scene setup becomes an implicit API. Renames, prefab reuse, additive scenes, tests, and mode-specific scenes are fragile because required references are not visible at compile time or in prefab contracts.

Recommended next move: document a scene contract for each game mode, then introduce a `LevelRuntimeContext` that exposes typed references. Keep old globals as compatibility accessors during migration.

### AUD-006: Spawn Lifecycle Does Not Have a Shared Pooling Contract

Evidence:

- `EnemySpawner` directly instantiates minions, bosses, and battle royal contestants.
- `TrafficManager` directly instantiates vehicles and mutates prefab/controller state before spawn.
- Enemy/bodyguard controllers destroy themselves on death/knockdown paths.
- Projectiles already have `ProjectilePool` and `PooledProjectile`, so a local pooling pattern exists.

Why it matters: mixed instantiate/destroy behavior can cause allocation spikes, stale references, and inconsistent reset behavior. Mutating prefab/component state before instantiate is especially risky because authored prefab defaults can accidentally become runtime state.

Recommended next move: document reset requirements for projectiles first, then create a generic pooled actor contract for enemies or vehicles as the first non-projectile pool.

### AUD-007: Animation Events Still Own Gameplay Timing

Evidence:

- `PlayerAnimationEvents` enables/disables hitboxes and attack boxes, spawns projectiles, applies force, changes Rigidbody kinematic state, and plays audio.
- `EnemyAnimationEvents` and `BodyGuardAnimationEvents` duplicate many of the attack box, projectile, force, and audio responsibilities.

Why it matters: animation clips become hidden gameplay scripts. Missing events, renamed methods, or animation timing edits can break attacks, projectile firing, or cleanup.

Recommended next move: define animation event methods as presentation/timing adapters only. Combat drivers should own attack windows, validate that an attack is still legal, and forcibly close windows on disable, death, interruption, and state exit.

### AUD-008: Basketball Flow Mixes Gameplay, UI, Stats, and Analytics

Evidence:

- `BasketBall` holds `GameStats`, `PlayerController`, UI `Text`, shot distance, shot state, collision handling, score text formatting, shot math, and analytics calls.
- `GameRules` also formats basketball score output; percentage formatting has been decoupled from `BasketBall.instance`, but score-output ownership is still duplicated.
- Shot attempts update state, stats, marker rules, player animation, shot meter messaging, and analytics in one flow.

Why it matters: shot behavior is a core game loop, but it is hard to test because one shot can affect physics, UI, scoring, progression stats, analytics, and mode rules directly.

Recommended next move: document the shot lifecycle and introduce a `ShotResult` event. UI, stats, analytics, progression, and rules should consume the result instead of being embedded in the shot execution path.

### AUD-009: Persistence and Progression Boundaries Need Ownership Rules

Evidence:

- `Pause` handles game pausing, high score persistence, all-time stat persistence, and match progression application.
- `DBConnector`, `DBHelper`, `PlayerData`, local account scripts, server messages, and progression services all participate in durable state.
- Progression has a stronger service shape than older persistence paths, but the source-of-truth rules are not documented yet.

Why it matters: save timing, offline behavior, retries, migration, and duplicate result prevention are product-critical. They should not depend on UI/pause flow details.

Recommended next move: document the persistence lifecycle from match result to local save/server sync. Promote progression result application behind a single service boundary and make pause/end-round UI call that boundary.

Additional evidence (player identifier / game mode audit pass): `Pause.updateFreePlayStats()` (`Assets/Scripts/game manager/Pause.cs:285-308`) independently builds its own `ProgressionService`/`MatchProgressionResult`/result-id (`freePlayProgressionResultId`) with logic nearly identical to `GameRules.ApplyMatchProgressionResult()` (`Assets/Scripts/game manager/GameRules.cs:315-328`, `matchProgressionResultId`) - two independent progression call sites with no shared boundary, confirming this finding rather than introducing a new one.

### AUD-013: Player Identity/Role Flags Have No Single Owner

Evidence:

- `PlayerIdentifier.isCpu`/`isDefensivePlayer` (`Assets/Scripts/player/PlayerIdentifier.cs`) are duplicated as independent fields on `PlayerController.isCPU` and `AutoPlayerController.isCPU`/`isDefensivePlayer`.
- Most gameplay code reads `PlayerIdentifier.isCpu`; the known `BasketBall.cs` and `groundcheck.cs` external reads that used controller-owned duplicate fields have been routed back through `PlayerIdentifier`.
- Each player slot has two separate `PlayerIdentifier` instances (actor and basketball object), manually kept in sync via calls like `_basketball1.setIds(_player1.pid, _player1.pid, _player1.pid, false)` (`Assets/Scripts/game manager/GameLevelManager.cs:413`).
- `bid`/`bsid` fields on `PlayerIdentifier` are always set equal to `pid` at all 17 `setIds` call sites and are never read anywhere - dead fields that look like they should diverge from `pid` but never do.

Why it matters: nothing keeps the duplicated flags in sync. A new spawn/mode path that sets one copy and forgets the other silently splits CPU-detection or defensive behavior between subsystems with no validation, and is a specific case of the broader AUD-003/AUD-004 "no single owner" pattern applied to player identity instead of health.

**2026-08-31: closed.** The controller-owned duplicate flags and `PlayerIdentifier.bid`/`bsid` were already deleted (2026-08-14 remainder pass, see the summary table row above). The remaining structural half - two `PlayerIdentifier` instances per slot, hand-synced - is fixed: a new basketball-domain contract, `IBasketballRuntime` (`Assets/Scripts/basketball/IBasketballRuntime.cs`), is implemented directly on `BasketBall`/`BasketBallAuto` and carries exactly what the ball needs (runtime participant id, CPU role, primary status, owner actor, `IShooterActor`, `BasketBallState`, `GameStats`, last-shot distance). `SpawnCoordinator.GiveBall` binds it explicitly (`runtime.BindOwner(...)`) immediately after `Instantiate` and before Unity calls `Start()` on any of the ball's components, replacing the old `ballIdentifier.setIds`/`setPlayer`/`setAutoPlayer`/`setBasketball`/`setAutoBasketball` hand-sync calls. `BasketBall`, `BasketBallAuto`, `BasketballState`, `BasketBallShotMade` (now `shotMade(IBasketballRuntime)`), and `BasketBallShotMadeCollision` no longer reference `PlayerIdentifier` at all, and `PlayerIdentifier` was removed from both `basketball.prefab` and `basketballAuto.prefab` via `PrefabUtility` (not hand-edited YAML) - a clean, single-component removal in each, verified against no other serialized reference. The actor-side `PlayerIdentifier` (reached through `PlayerRegistry`) remains the single authoritative participant identity.

One additional, previously-undocumented consumer surfaced during this pass: `groundcheck.cs`'s `GroundCheck` component is authored as a child both of the player/autoPlayer actor *and* of the basketball object itself, and the ball-attached instance was walking up to the ball's own duplicate `PlayerIdentifier` via `GetComponentInParent`. This was not visible to a search scoped to `Assets/Scripts/basketball` (the script lives under `Assets/Scripts/player`) and only surfaced as a PlayMode test regression (`GroundCheck.Start()` NRE) after the prefab component was removed. Fixed the same way: it now falls back to `GetComponentInParent<IBasketballRuntime>()` when no `PlayerIdentifier` is found in its parent hierarchy, leaving the player/autoPlayer-attached path (which still finds a real `PlayerIdentifier`) completely unchanged.

Guarded permanently by `Level5BasketballRuntimeIdentityGuardTests` (zero `PlayerIdentifier` references in the five migrated files; zero `PlayerIdentifier` components in either ball prefab) and exercised by `Level5BasketballOwnershipBindingTests` (8 tests driving the real `SpawnCoordinator.GiveBall` and production prefabs). Verified: full compile + 714/714 EditMode + 13/13 PlayMode (Unity 6000.5.7f1 batch mode), `validate-repository.ps1` clean.

### AUD-014: Player Slot Index Is Implicitly Reused As a Save-Data Key

Evidence:

- Original evidence: `HighScoreModel.convertBasketBallStatsToModel` (`Assets/Scripts/Models/HighScoreModel.cs:172-181`) looped over `PlayerIdentifier` entries and did `if (!p.isCpu) index = p.pid;`, then indexed back into the same list (`pi[index]`, lines 214-274) to build the saved/highscore stat model.
- With more than one non-CPU entry, that loop kept overwriting `index`, so only the last human player's stats were saved.
- `cameraUpdater.cs:101` (`x.pid == 0`) and `GameRules.cs:420-463` (`players[0].pid + 1` for display) make the same implicit assumption that `pid` always equals list position, with nothing enforcing it.

Why it mattered: this was a save/highscore correctness bug, not just an architecture smell - any path that legitimately had more than one human player entry, or any future change that let `pid` and list position diverge, silently recorded the wrong player's stats.

Resolution: `convertBasketBallStatsToModel` now selects a `PlayerIdentifier` object directly through `GetPrimaryPlayer` and uses `TryGetPlayer` for bounded leaderboard slot reads.

### AUD-015: Basketball Shot-Attempt Flags Never Reset on a Miss

Evidence:

- `TwoAttempt`/`ThreeAttempt`/`FourAttempt`/`SevenAttempt` are set `true` in `updateBasketBallStateShotTypeOnShoot` (`Assets/Scripts/basketball/BasketBall.cs:337-360`, duplicated in `BasketBallAuto.cs:371-394`).
- They are only ever reset to `false` inside the make path, `BasketBallShotMade.shotMade`/`updateShotMadeBasketBallStats` (`Assets/Scripts/basketball/BasketBallShotMade.cs:114-117`, `333-336`).
- `BasketBall.OnCollisionEnter` (`BasketBall.cs:184-218`) resets `Locked`/`Thrown`/`CanPullBall` on a miss but never the attempt flags.

Why it matters: a player who misses a 2pt attempt, moves to 3pt range, and makes that shot has both `TwoAttempt` (stale) and `ThreeAttempt` (current) still `true` when `shotMade()` runs, so both categories get scored for the same made shot (`BasketBallShotMade.cs:188-234`). This is a reachable scoring-integrity bug, not a theoretical one.

Recommended next move: reset all shot-type attempt flags at the start of every new attempt (or explicitly on miss detection), not only inside the make path.

### AUD-016: Basketball Shooting Percentages Can Be Computed From the Wrong Player

Evidence:

- Original evidence: `GameRules.GetStatsTotals()` (`Assets/Scripts/game manager/GameRules.cs:955-1003`) took made/attempt counts from `gameStats1` (a specific player's stats, set once at `GameRules.cs:127`) but took percentages from `BasketBall.instance.getXAccuracy()`.
- `BasketBall.instance.gameStats` is reassigned inside `BasketBall.updateScoreText()` (`BasketBall.cs:655`) to `GameLevelManager.instance.players[0].gameStats` - i.e. whichever player's basketball object last ran a score update.
- `GameRules.cs:925-934` shows local-multiplayer branches exist (`numPlayers > 1..3`), so this path is reachable outside 1-player games.

Why it mattered: in local-multiplayer modes, a displayed shooting percentage for one player could silently be computed from a different player's accuracy state while the made/attempt counts next to it were correct - a subtle stats-display bug that would be hard to notice without deliberately comparing numbers.

Resolution: `GetStatsTotals` now computes all percentages from `gameStats1` made/attempt counts via `UtilityFunctions.getPercentageFloat`, not through `BasketBall.instance`.

### AUD-017: `BasketBall` and `BasketBallAuto` Duplicate the Entire Shot Pipeline

Evidence:

- `shootBasketBall`, `Launch`, `updateBasketBallStateShotTypeOnShoot`, `getAccuracyModifier`, `getRangeModifier`, `getReleaseModifier`, `updateScoreText`, and all `getXAccuracy` methods are copy-pasted between `BasketBall.cs` and `BasketBallAuto.cs` (e.g. `BasketBall.cs:532-561` vs `BasketBallAuto.cs:623-652`).
- The two paths have already diverged: `BasketBallAuto.cs:328-348` is missing the money-ball/marker-attempt block present in the human path, and the meter-wait condition differs (`MeterEnded == true` vs `== false`, `BasketBall.cs:493`).
- `Launch()` (`BasketBall.cs:365-483`) itself mixes projectile physics, RNG-based crit/accuracy/range/release rolls, shot-meter UI string building, and an `AnaylticsManager.PlayerShoot` analytics call in one method - the specific coupling point behind the existing AUD-010 finding.

Why it matters: every shot-logic fix, including AUD-015 and AUD-016, has to be applied twice or the human/CPU paths silently diverge further than they already have.

Recommended next move: extract the shared shot pipeline (physics, modifiers, scoring) into one code path parameterized by actor, with only genuine CPU-vs-human differences (input source) kept separate. This deepens AUD-010 rather than replacing it.

### AUD-018: Game Mode Dispatch Is Duplicated With Inconsistent Constants

Evidence:

- `GameRules.SetScoreDisplayText` (`Assets/Scripts/game manager/GameRules.cs:578-839`, ~20 branches) and `GameRules.GetDisplayText` (`GameRules.cs:855-943`, ~20 branches) each implement their own if-chain over the same mode set.
- `SetScoreDisplayText` uses named `Modes.*` constants; `GetDisplayText` uses raw integer literals for the same modes (e.g. `gameModeId == 15 || ... || gameModeId == 24 || gameModeId == 27`, `GameRules.cs:879`) and mixes range checks with literal equality in the same expression.
- Even within `GameRules.Update()`, raw literals `26`/`23` appear at lines 244/248 while the `Modes.FreePlay`/`Modes.BeatThaComputahs` constants are used two lines later at line 266.

Why it matters: adding, renaming, or reordering a mode requires editing two independently-maintained literal representations that can silently drift out of sync, and the mixed literal/constant style makes it easy to miss a spot when they do.

Recommended next move: replace both methods with a single per-mode lookup (data table or strategy) keyed off `Modes` constants only; remove raw integer literals from mode checks entirely.

### AUD-019: Match-End Side Effects Run on a Per-Frame Poll

Evidence:

- **Correction (2026-08-02):** the original version of this finding claimed `GameRules.IsGameOver()` was dead code. That was wrong - it's live, marker-depletion-based win condition logic (`GameRules.cs:1022-1051`, `MarkersRemaining <= 0`) called from `BasketBallShotMarker.cs:125` and `:145` for contest/marker modes. `Timer.Update()`'s time-based path (`Assets/Scripts/game manager/Timer.cs:130-163`) is a separate, legitimate mechanism for timed modes, not a competing/duplicate path for the same modes. There is no evidence these two conflict for any single mode.
- What did still hold: `GameRules.cs` read `GameLevelManager.instance.PlayerHealth.IsDead` directly as a game-over trigger inside a per-frame `Update()` check, which also ran the entire end-of-match flow (pause, persistence, progression application, campaign scene-load) as part of that same per-frame poll rather than firing once.
- `Timer.Update()` also reimplements consecutive-shot-streak logic (`Timer.cs:156-157`) that duplicates flag plumbing already in `GameRules` - worth a look, but not urgent.

Resolution: `GameRules` now has a single match-end entry path. `Timer` and shot-marker depletion call `RequestGameOver()`, player death also raises `RequestGameOver()` through `PlayerHealth.OnDied`, and `HandleMatchEnded()` is protected by re-entry and per-side-effect idempotency guards so pause, persistence, progression, and campaign transition side effects do not duplicate. Progression now applies from primary `GameStats` independently of DB availability. The old polling fallback for `PlayerHealth.IsDead` remains as a compatibility safety net, but it no longer owns the side effects directly.

Recommended next move: add a play-mode smoke test that triggers timer expiry, marker completion, and player death, then asserts the match-end save/progression path runs once per match.

### AUD-020: Traffic Vehicles Mutated Shared Prefab State Before Instantiating

Found and fixed independently of the combat-contracts/match-end work above - confirmed zero references to `GameRules`, `Timer`, `Pause`, `LoadGame`, or `PlayerHealth` anywhere in `Assets/Scripts/vehicle/` before starting.

Evidence:

- `TrafficManager.loadVehiclePrefabs()`/`loadCustomVehiclePrefabs()` (`Assets/Scripts/vehicle/TrafficManager.cs`) populate `VehiclesList` with `VehicleController` components read directly off prefab assets (via `Resources.LoadAll` or the custom prefab list), not runtime clones.
- `spawnVehiclePrefabs()`, `spawnCustomVehiclePrefabs()`, and `spawnVehicleCoRoutine()` all set `Direction`/`FacingRight`/`CurrentTarget` directly on those shared `VehiclesList` entries, then call `Instantiate()` - mutating shared/prefab-sourced state before spawn, the exact pattern AUD-009 already flagged generically. The original developer left a comment acknowledging it: `// need to be spawning from prefabs list. this is saving and changing prefabs value`.
- `spawnVehicleCoRoutine` mutated the shared reference, then `yield return new WaitForSeconds(waitTimeToRespawn)` before instantiating - if the same vehicle ID's coroutine fired twice in quick succession (two vehicles of that type despawning close together), both would mutate the same shared `VehicleController` reference while waiting, and whichever `WaitForSeconds` resolved second would silently overwrite the first's pending `Direction`/`CurrentTarget` before either had instantiated.
- Confirmed via `VehicleController.Start()` that `facingRight` is unconditionally recomputed from `transform.localScale.x` regardless of what's set beforehand - so `FacingRight` assignments in `TrafficManager` were already inert. Left as-is (out of scope, not the bug this pass targets).

Why it matters: a save-data-adjacent-severity bug in spirit to AUD-014/AUD-015 - shared mutable state written by more than one code path with timing that can race, silently producing a vehicle with the wrong direction/target.

Resolution: all three call sites now instantiate first and set `Direction`/`FacingRight`/`CurrentTarget` on the returned clone, never on the shared `VehiclesList`/prefab reference. Grepped the whole codebase first to confirm `VehicleController`/`VehiclesList` have no consumers outside `vehicle/TrafficManager.cs` and `vehicle/VehicleController.cs`, so narrowing this fix to those two files was safe.

### AUD-021: Beat the Computahs Continues Never Reset Between Campaign Attempts

Requested by the user directly: "audit the 'beat the computahs' mode... could probably use better game design." The user confirmed ties are an intentional mechanic, not a bug (not touched). The exact intended continues count is unconfirmed/doesn't matter for this fix - left at 2, the pre-existing default; the bug is the missing reset, not the count.

Evidence:

- `EndRoundData.numberOfContinues` (`Assets/Scripts/menu_start/EndRoundData.cs:11`, was `static public int numberOfContinues = 2;`) is a static field, meaning it persists for the life of the application process, not per campaign attempt.
- The only writes to it: decremented on a loss-with-continues-remaining (`EndRoundMenuManager.cs:240`), and zeroed for hardcore mode (`StartManager.cs:1437`, inside `setGameOptions()` which runs at the start of every game). Nothing reset it back to the default for a fresh non-hardcore run.
- Also traced a related, currently-harmless architecture smell while investigating: `GameRules.LoadNextCampaignLevel()` always optimistically advances `GameOptions.levelSelectedIndex` before the match outcome is known; `EndRoundMenuManager.LoadData()` rolls it back on a tie or a loss-with-continues, but not on a loss with zero continues left. That gap is currently masked only because `StartManager.cs:1273` hard-resets `levelSelectedIndex` to level 1 every time "Beat the Computahs" is freshly started - an incidental safety net, not a designed invariant. Not fixed this pass (no live bug found), but worth documenting as a latent footgun for any future "resume where you left off" feature.

Why it matters: a player who exhausted their continues once had zero continues on every subsequent "Beat the Computahs" attempt for the rest of the session - a real, silent, session-persistent progression bug, not just an architecture smell.

Resolution: extracted `EndRoundData.DefaultContinues = 2` as a named constant (unchanged value - the pre-existing default, not a confirmed design number). `StartManager.setGameOptions()` now does `EndRoundData.numberOfContinues = hardcoreEnabled ? 0 : EndRoundData.DefaultContinues;` on every fresh game start, instead of only ever zeroing it for hardcore mode.

## Recent Mitigations

These issues were reduced by the recent player interaction work, but they should remain visible until durable architecture follow-up is complete.

- Shared combat contracts were introduced with `DamageInfo`, `IDamageable`, `ICombatAgent`, and `CombatReservation`.
- Player, enemy, and bodyguard health now expose a common damageable contract.
- Enemy and bodyguard health now clamp health, guard duplicate death transitions, and publish health/death events.
- Enemy and bodyguard health bars now subscribe to health change events instead of relying only on manual refresh calls.
- Enemy/bodyguard collision damage and enemy lightning damage now route through `ApplyDamage(DamageInfo)` and use the returned death result for cleanup.
- `PlayerAttackQueue` now exposes reservation-oriented APIs, read-only queue state, first-target/slot accessors, and reservation release methods.
- Enemy and bodyguard controllers now implement `ICombatAgent` and release queue reservations on disable/death paths.
- Player health and death were hardened so dead players ignore additional damage and publish clearer state.
- Player attack queue release paths were improved to reduce stuck attacker reservations.
- Projectile pooling and reset behavior were improved.
- Player movement was adjusted toward Rigidbody-friendly Unity patterns.
- Animation event entry points were made safer, but animation events still need stronger system-level contracts.

## Recommended Remediation Order

1. Define common combat contracts first: `IDamageable`, `DamageInfo`, health/death events, attack definitions, and combat agent/reservation interfaces.
2. Refactor one enemy melee path through those contracts to prove the shape.
3. Convert the player attack queue into a generic combat slot reservation system.
4. Move player health UI updates behind health/death events.
5. Document and stabilize projectile reset rules, then apply pooling to the next highest-churn runtime object type.
6. Write focused edit-mode tests for health/death, damage application, reservation release, and projectile reset.
7. Split `PlayerController` only after the combat contracts are stable enough to reduce risk.

## Audit Backlog

- Build a combat sequence diagram from input/AI decision through hit resolution and death cleanup.
- Audit all `FindObjectOfType`, `GameObject.Find`, static singleton, and global option usage.
- Inventory all `Instantiate`/`Destroy` hot paths and classify which need pooling.
- Identify every animation event method and document which ones are critical gameplay triggers.
- Trace basketball scoring from shot start through stats/progression/end-round output.
- Trace account/progression persistence from menu selection through local/server save.
- Classify scripts as core runtime, mode-specific runtime, UI, persistence, dev-only, test, or legacy.
- Add basketball scoring regression coverage for AUD-014/AUD-015/AUD-016 so these closed fixes stay locked down.
- ~~Reconcile the duplicate progression call sites in `Pause.updateFreePlayStats()` and `GameRules.ApplyMatchProgressionResult()` per AUD-011.~~ Done 2026-08-13: both now call a shared `ProgressionService.ApplyMatchResult(resultId, characterId, experienceGained)` overload instead of each constructing `MatchProgressionResult` by hand. Investigated first: both already source their result id from the same `MatchSession.EnsureCurrentMatch()`, and `DBConnector.ApplyProgressionResult` is idempotent per result id, so this was a DRY cleanup, not a live double-grant bug - worth recording so it isn't re-investigated as one.
- Decide whether `LevelSelected` (runtime) or `LevelPreset`/`LevelCatalog` (used only by `UnlockService`) should be the single level data model - currently both exist and are kept in sync by hand.
- **AUD-068 (new 2026-08-14, fixed same day on explicit instruction to proceed despite the ambiguity below).** `GameOptions.tipDialogueLoadedOnStart` (`Assets/Scripts/menu_start/GameOptions.cs:130`) is a `static bool` set `true` in `Start()` by both `StartScreenTipDialogueManager` and `StartScreenCpuSelectManager`, and grepped across the whole tree with no reset anywhere - unlike every sibling per-match `GameOptions` static near it. Whether "show once per app launch" was the actual original intent (the name reads that way) or a bug in the AUD-021 shape (a static nobody remembered to reset) could not be determined from the repository alone; the asymmetry below was read as evidence for the latter (a consistent "once per app launch" design would not have one of its two guards commented out) and, on the user explicitly asking for it to be fixed anyway, `StartManager.applyNonMatchLaunchState()` now resets `GameOptions.tipDialogueLoadedOnStart = false` right next to the identically-shaped `EndRoundData.numberOfContinues` reset (AUD-021) - at match-launch time rather than mid-flow, so it's correct the *next* time a player re-enters the start flow without disturbing whichever of the two screens already ran during the flow that's launching. Separately: `StartScreenCpuSelectManager.Awake()`'s own guard around the same flag was already commented out while its `OnEnable()`'s gate on `PlayerControlsProvider.EnableMenuMaps()` was still live; `OnEnable()` now always enables menu maps too, matching `Awake()`'s already-unconditional wiring instead of the mismatched pair. Verified by full compile + 401/401 EditMode + 4/4 PlayMode + `validate-repository.ps1`; **this is a menu-flow behavior change with no automated coverage - worth a manual check** (go through the start flow, complete a match, go through it again, confirm the tip/CPU-select screens behave as expected the second time) before shipping.
- **Housekeeping found, not fixed (2026-08-14).** `Assets/Scripts/player_racing/RacingVehicleCollisions.cs` is not attached to any prefab or scene anywhere in the repository (grepped `Assets/**/*.prefab` and `Assets/**/*.unity`) and nothing calls `AddComponent<RacingVehicleCollisions>` - the class is entirely unreachable at runtime as the repository currently stands. Its live `OnTriggerEnter` also unconditionally calls `playerKnockedDown()` on any obstacle collision, ignoring the `[SerializeField] bool playerCanBeKnockedDown` it still declares (the only place that field is read is inside a large commented-out block implementing a different, more elaborate damage/knockdown/blocking model that no longer matches the live method's shape) - but since the component is never attached to anything, this is not a live bug, just dead code left in a state that would be wrong if it were ever wired back up. Not deleted: `player_racing` is a real, still-referenced minigame (`RacingGameManager`, `RacingVehicleController` etc. are all live), so whether this specific file is abandoned-and-safe-to-delete or mid-migration-and-about-to-be-wired-up is a question about the minigame's roadmap, not something the repository answers by itself.
- ~~Housekeeping found in `BehaviorVehicleLawnmower.cs` and `DBHelper.UpdatePlayerProfileProgression`.~~ Both fixed 2026-08-15 on explicit instruction to fix despite being currently-unreachable/dead-callsite findings - see AUD-075 and AUD-076.
