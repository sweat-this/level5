# Level 5 — Claude Code

@AGENTS.md

The repository-wide engineering requirements in `AGENTS.md` are authoritative.

For routine AI-assisted work:

1. Start from [`docs/ai/PRESETS.md`](docs/ai/PRESETS.md).
2. Choose the matching route (`status`, `plan`, `implement`, `bug`, `review`) and mode (`lean`, `standard`, `deep`).
3. Inspect targeted repository evidence for that route and mode.
4. Load detailed workflow files under `docs/ai/skills/` only when the preset, risk, or task requires them.

Do not maintain duplicate copies of `AGENTS.md` or `docs/ai/PRESETS.md` rules here.
